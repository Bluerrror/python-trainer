/* Python Trainer — course content (en / de / fa) */

const UNITS=[

/* ================= 1 · BASICS ================= */
{ic:'🐍',col:'#306698',lv:'START',
 title:{en:'Python Basics',de:'Python-Grundlagen',fa:'مبانی پایتون'},
 desc:{en:'Variables, numbers, strings & print',de:'Variablen, Zahlen, Strings & print',fa:'متغیرها، اعداد، رشته‌ها و print'},
 blocks:[
  {note:{en:'<b>Variables</b> store values. You create one simply by assigning with <code>=</code>. Python figures out the type by itself — <code>int</code>, <code>float</code>, <code>str</code> or <code>bool</code>.',
         de:'<b>Variablen</b> speichern Werte. Du erzeugst sie einfach mit <code>=</code>. Python erkennt den Typ selbst — <code>int</code>, <code>float</code>, <code>str</code> oder <code>bool</code>.',
         fa:'<b>متغیرها</b> مقدارها را نگه می‌دارند. فقط با <code>=</code> ساخته می‌شوند. پایتون نوع را خودش تشخیص می‌دهد — <code>int</code>، <code>float</code>، <code>str</code> یا <code>bool</code>.'}},
  {code:'age = 34            # int\nprice = 9.99        # float\nname = "Ada"        # str\nhappy = True        # bool\n\nprint(name, "is", age)\nprint(type(price))',label:'variables.py'},
  {note:{en:'<b>Numbers & math.</b> Python is a great calculator: <code>+ - * /</code>, integer division <code>//</code>, remainder <code>%</code> and power <code>**</code>.',
         de:'<b>Zahlen & Mathe.</b> Python ist ein toller Taschenrechner: <code>+ - * /</code>, Ganzzahldivision <code>//</code>, Rest <code>%</code> und Potenz <code>**</code>.',
         fa:'<b>اعداد و ریاضی.</b> پایتون ماشین‌حساب خوبی است: <code>+ - * /</code>، تقسیم صحیح <code>//</code>، باقیمانده <code>%</code> و توان <code>**</code>.'}},
  {code:'print(7 + 3, 7 - 3, 7 * 3)\nprint(7 / 2)     # 3.5  (always float)\nprint(7 // 2)    # 3    (floor division)\nprint(7 % 2)     # 1    (remainder)\nprint(2 ** 10)   # 1024 (power)',label:'math.py'},
  {note:{en:'<b>Strings</b> hold text. Use <b>f-strings</b> to mix values into text: put an <code>f</code> before the quote and write variables in <code>{}</code>.',
         de:'<b>Strings</b> enthalten Text. Mit <b>f-Strings</b> mischst du Werte in Text: ein <code>f</code> vor dem Anführungszeichen, Variablen in <code>{}</code>.',
         fa:'<b>رشته‌ها</b> متن نگه می‌دارند. با <b>f-string</b> مقدارها را داخل متن بگذار: یک <code>f</code> قبل از نقل‌قول و متغیرها داخل <code>{}</code>.'}},
  {code:'name = "Ada"\nlang = "Python"\nyears = 3\n\nprint(f"{name} has used {lang} for {years} years")\nprint(f"Next year: {years + 1}")\nprint(name.upper(), len(name))',label:'strings.py'},
  {note:{en:'Try changing the numbers and text above, then press <b>▶ Run</b> again — this is a real Python running on your device!',
         de:'Ändere oben Zahlen und Text und drücke wieder <b>▶ Run</b> — das ist echtes Python auf deinem Gerät!',
         fa:'اعداد و متن بالا را عوض کن و دوباره <b>▶ Run</b> بزن — این پایتون واقعی روی دستگاه توست!'}},
 ],
 quiz:[
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'x = 7\ny = 2\nprint(x // y)',
   opts:['3.5','3','4','7.2'],a:1,
   ex:{en:'<code>//</code> is floor division: 7 // 2 = 3.',de:'<code>//</code> ist Ganzzahldivision: 7 // 2 = 3.',fa:'<code>//</code> تقسیم صحیح است: 7 // 2 = 3.'}},
  {q:{en:'Which type does <code>3.0</code> have?',de:'Welchen Typ hat <code>3.0</code>?',fa:'نوع <code>3.0</code> چیست؟'},
   opts:['int','float','str','bool'],a:1,
   ex:{en:'A decimal point makes it a float.',de:'Der Dezimalpunkt macht es zu float.',fa:'نقطه اعشار آن را float می‌کند.'}},
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'name = "Py"\nprint(f"Hi {name}{name}!")',
   opts:['Hi PyPy!','Hi Py Py!','Hi {name}{name}!','Error'],a:0,
   ex:{en:'f-strings replace each {name} with its value.',de:'f-Strings ersetzen jedes {name} durch den Wert.',fa:'f-string هر {name} را با مقدارش جایگزین می‌کند.'}},
  {q:{en:'What is <code>2 ** 3</code>?',de:'Was ist <code>2 ** 3</code>?',fa:'حاصل <code>2 ** 3</code> چیست؟'},
   opts:['6','8','9','23'],a:1,
   ex:{en:'<code>**</code> is the power operator: 2·2·2 = 8.',de:'<code>**</code> ist Potenz: 2·2·2 = 8.',fa:'<code>**</code> عملگر توان است: ۲×۲×۲=۸.'}},
  {q:{en:'What does <code>len("hello")</code> return?',de:'Was liefert <code>len("hello")</code>?',fa:'<code>len("hello")</code> چه برمی‌گرداند؟'},
   opts:['4','5','6','"hello"'],a:1,
   ex:{en:'len() counts the characters — h e l l o = 5.',de:'len() zählt die Zeichen — h e l l o = 5.',fa:'len() تعداد حروف را می‌شمارد — ۵ حرف.'}},
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'print(10 % 3)',
   opts:['3','1','0','3.33'],a:1,
   ex:{en:'<code>%</code> gives the remainder: 10 = 3·3 + 1.',de:'<code>%</code> gibt den Rest: 10 = 3·3 + 1.',fa:'<code>%</code> باقیمانده است: ۱۰ = ۳×۳ + ۱.'}},
 ]},

/* ================= 2 · COLLECTIONS ================= */
{ic:'🧺',col:'#2b8a78',lv:'BASICS',
 title:{en:'Lists & Dicts',de:'Listen & Dicts',fa:'لیست‌ها و دیکشنری‌ها'},
 desc:{en:'Lists, slicing, dicts, tuples & sets',de:'Listen, Slicing, Dicts, Tupel & Sets',fa:'لیست، برش، دیکشنری، تاپل و مجموعه'},
 blocks:[
  {note:{en:'A <b>list</b> is an ordered collection. Index from 0, use negative indexes from the end, and <b>slice</b> with <code>[start:stop]</code> (stop is excluded).',
         de:'Eine <b>Liste</b> ist eine geordnete Sammlung. Index ab 0, negative Indizes zählen vom Ende, <b>Slicing</b> mit <code>[start:stop]</code> (stop ist exklusiv).',
         fa:'<b>لیست</b> مجموعه‌ای مرتب است. اندیس از ۰ شروع می‌شود، اندیس منفی از انتها می‌شمارد و <b>برش</b> با <code>[start:stop]</code> است (stop شامل نمی‌شود).'}},
  {code:'fruits = ["apple", "banana", "cherry", "date"]\n\nprint(fruits[0])      # first\nprint(fruits[-1])     # last\nprint(fruits[1:3])    # slice\n\nfruits.append("fig")\nprint(len(fruits), fruits)',label:'lists.py'},
  {note:{en:'A <b>dict</b> maps keys to values — perfect for structured data. A <b>tuple</b> is like a list but immutable; a <b>set</b> keeps only unique values.',
         de:'Ein <b>Dict</b> bildet Schlüssel auf Werte ab — perfekt für strukturierte Daten. Ein <b>Tupel</b> ist wie eine Liste, aber unveränderlich; ein <b>Set</b> enthält nur eindeutige Werte.',
         fa:'<b>دیکشنری</b> کلید را به مقدار وصل می‌کند — عالی برای داده ساخت‌یافته. <b>تاپل</b> مثل لیست ولی تغییرناپذیر است؛ <b>مجموعه</b> فقط مقادیر یکتا دارد.'}},
  {code:'person = {"name": "Ada", "age": 36}\nprint(person["name"])\nperson["city"] = "London"\nprint(person)\n\npoint = (3, 4)          # tuple\nnums = {1, 2, 2, 3, 3}  # set\nprint(point[0], nums)',label:'dicts.py'},
  {code:'squares = [1, 4, 9, 16, 25, 36]\nprint(squares[::2])    # every 2nd\nprint(squares[::-1])   # reversed\nprint(9 in squares)    # membership\nprint(sum(squares), max(squares))',label:'slicing.py'},
 ],
 quiz:[
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'a = [10, 20, 30, 40]\nprint(a[-1])',
   opts:['10','40','30','Error'],a:1,
   ex:{en:'Index -1 is the last element.',de:'Index -1 ist das letzte Element.',fa:'اندیس ۱- آخرین عنصر است.'}},
  {q:{en:'What does <code>[1,2,3,4][1:3]</code> give?',de:'Was ergibt <code>[1,2,3,4][1:3]</code>?',fa:'<code>[1,2,3,4][1:3]</code> چه می‌دهد؟'},
   opts:['[1, 2, 3]','[2, 3]','[2, 3, 4]','[1, 2]'],a:1,
   ex:{en:'Slices include start, exclude stop: indexes 1 and 2.',de:'Slices: start inklusiv, stop exklusiv — Index 1 und 2.',fa:'برش start را شامل و stop را حذف می‌کند: اندیس ۱ و ۲.'}},
  {q:{en:'How do you read the value of key <code>"age"</code>?',de:'Wie liest man den Wert des Schlüssels <code>"age"</code>?',fa:'مقدار کلید <code>"age"</code> را چگونه می‌خوانی؟'},code:'d = {"name": "Ada", "age": 36}',
   opts:['d.age','d["age"]','d(age)','d{age}'],a:1,
   ex:{en:'Dict values are read with square brackets: d["age"].',de:'Dict-Werte liest man mit eckigen Klammern: d["age"].',fa:'مقدار دیکشنری با کروشه خوانده می‌شود: d["age"].'}},
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'s = {1, 2, 2, 3, 3, 3}\nprint(len(s))',
   opts:['6','3','1','Error'],a:1,
   ex:{en:'Sets keep only unique values: {1, 2, 3}.',de:'Sets behalten nur eindeutige Werte: {1, 2, 3}.',fa:'مجموعه فقط مقادیر یکتا را نگه می‌دارد: {1,2,3}.'}},
  {q:{en:'Which one CANNOT be changed after creation?',de:'Was kann nach dem Erstellen NICHT geändert werden?',fa:'کدام‌یک بعد از ساخت قابل تغییر نیست؟'},
   opts:['list','dict','tuple','set'],a:2,
   ex:{en:'Tuples are immutable.',de:'Tupel sind unveränderlich.',fa:'تاپل تغییرناپذیر است.'}},
  {q:{en:'What does <code>[1,2,3][::-1]</code> give?',de:'Was ergibt <code>[1,2,3][::-1]</code>?',fa:'<code>[1,2,3][::-1]</code> چه می‌دهد؟'},
   opts:['[1, 2, 3]','[3, 2, 1]','[1, 3]','Error'],a:1,
   ex:{en:'Step -1 walks the list backwards.',de:'Schritt -1 läuft rückwärts durch die Liste.',fa:'گام ۱- لیست را برعکس می‌کند.'}},
 ]},

/* ================= 3 · CONTROL FLOW ================= */
{ic:'🔀',col:'#b06325',lv:'BASICS',
 title:{en:'Control Flow',de:'Kontrollfluss',fa:'کنترل جریان'},
 desc:{en:'if / elif / else, for & while loops',de:'if / elif / else, for- & while-Schleifen',fa:'شرط‌ها و حلقه‌های for و while'},
 blocks:[
  {note:{en:'<b>if / elif / else</b> chooses a branch. Indentation (4 spaces) defines the block — no braces in Python!',
         de:'<b>if / elif / else</b> wählt einen Zweig. Die Einrückung (4 Leerzeichen) definiert den Block — keine Klammern in Python!',
         fa:'<b>if / elif / else</b> مسیر را انتخاب می‌کند. تورفتگی (۴ فاصله) بلوک را مشخص می‌کند — در پایتون آکولاد نداریم!'}},
  {code:'temp = 23\n\nif temp > 30:\n    print("hot 🥵")\nelif temp > 15:\n    print("nice 🙂")\nelse:\n    print("cold 🥶")',label:'if.py'},
  {note:{en:'<b>for</b> iterates over anything: lists, strings, or <code>range(n)</code> (0 … n-1). <code>enumerate</code> also gives you the index.',
         de:'<b>for</b> iteriert über alles: Listen, Strings oder <code>range(n)</code> (0 … n-1). <code>enumerate</code> liefert zusätzlich den Index.',
         fa:'<b>for</b> روی همه‌چیز می‌چرخد: لیست، رشته یا <code>range(n)</code> (از ۰ تا n-1). <code>enumerate</code> اندیس را هم می‌دهد.'}},
  {code:'for i in range(5):\n    print(i, i * i)\n\nfor idx, fruit in enumerate(["apple", "banana"]):\n    print(idx, fruit)',label:'for.py'},
  {note:{en:'<b>while</b> repeats as long as a condition is true. <code>break</code> exits the loop, <code>continue</code> skips to the next round.',
         de:'<b>while</b> wiederholt, solange eine Bedingung wahr ist. <code>break</code> verlässt die Schleife, <code>continue</code> springt zur nächsten Runde.',
         fa:'<b>while</b> تا وقتی شرط برقرار است تکرار می‌کند. <code>break</code> از حلقه خارج می‌شود و <code>continue</code> به دور بعد می‌رود.'}},
  {code:'n = 1\nwhile n < 100:\n    n = n * 2\n    if n == 32:\n        continue     # skip printing 32\n    print(n)\nprint("done, n =", n)',label:'while.py'},
 ],
 quiz:[
  {q:{en:'How many numbers does <code>range(4)</code> produce?',de:'Wie viele Zahlen erzeugt <code>range(4)</code>?',fa:'<code>range(4)</code> چند عدد تولید می‌کند؟'},
   opts:['3','4','5','1'],a:1,
   ex:{en:'range(4) → 0, 1, 2, 3 — four numbers.',de:'range(4) → 0, 1, 2, 3 — vier Zahlen.',fa:'range(4) یعنی ۰،۱،۲،۳ — چهار عدد.'}},
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'x = 5\nif x > 10:\n    print("big")\nelif x > 3:\n    print("medium")\nelse:\n    print("small")',
   opts:['big','medium','small','nothing'],a:1,
   ex:{en:'5 is not > 10 but is > 3 → "medium".',de:'5 ist nicht > 10, aber > 3 → "medium".',fa:'۵ بزرگ‌تر از ۱۰ نیست ولی از ۳ هست → medium.'}},
  {q:{en:'What ends a loop immediately?',de:'Was beendet eine Schleife sofort?',fa:'چه چیزی حلقه را فوراً تمام می‌کند؟'},
   opts:['continue','pass','break','stop'],a:2,
   ex:{en:'<code>break</code> exits the loop at once.',de:'<code>break</code> verlässt die Schleife sofort.',fa:'<code>break</code> بلافاصله از حلقه خارج می‌شود.'}},
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'total = 0\nfor i in range(1, 4):\n    total += i\nprint(total)',
   opts:['6','10','3','7'],a:0,
   ex:{en:'1 + 2 + 3 = 6 (range stops before 4).',de:'1 + 2 + 3 = 6 (range stoppt vor 4).',fa:'۱+۲+۳=۶ (پیش از ۴ متوقف می‌شود).'}},
  {q:{en:'Python marks code blocks with…',de:'Python markiert Codeblöcke mit …',fa:'پایتون بلوک کد را با چه مشخص می‌کند؟'},
   opts:['{ } braces','( ) parentheses','indentation','semicolons'],a:2,
   ex:{en:'Indentation is syntax in Python.',de:'Einrückung ist in Python Syntax.',fa:'تورفتگی در پایتون بخشی از دستور زبان است.'}},
  {q:{en:'What does <code>continue</code> do in a loop?',de:'Was macht <code>continue</code> in einer Schleife?',fa:'<code>continue</code> در حلقه چه می‌کند؟'},
   opts:['ends the loop','skips to the next iteration','restarts the program','pauses'],a:1,
   ex:{en:'It jumps straight to the next iteration.',de:'Es springt direkt zur nächsten Iteration.',fa:'مستقیم به تکرار بعدی می‌پرد.'}},
 ]},

/* ================= 4 · FUNCTIONS ================= */
{ic:'🧩',col:'#7b5fe0',lv:'BASICS',
 title:{en:'Functions',de:'Funktionen',fa:'توابع'},
 desc:{en:'def, return, lambda & comprehensions',de:'def, return, lambda & Comprehensions',fa:'def، return، lambda و comprehension'},
 blocks:[
  {note:{en:'<b>Functions</b> package code you want to reuse. Define with <code>def</code>, give back a result with <code>return</code>. Parameters can have <b>default values</b>.',
         de:'<b>Funktionen</b> verpacken wiederverwendbaren Code. Definiert mit <code>def</code>, Ergebnis mit <code>return</code>. Parameter können <b>Standardwerte</b> haben.',
         fa:'<b>تابع</b> کدی است که می‌خواهی دوباره استفاده کنی. با <code>def</code> تعریف و با <code>return</code> نتیجه برگردان. پارامترها می‌توانند <b>مقدار پیش‌فرض</b> داشته باشند.'}},
  {code:'def greet(name, lang="en"):\n    if lang == "de":\n        return f"Hallo {name}!"\n    return f"Hello {name}!"\n\nprint(greet("Ada"))\nprint(greet("Ada", lang="de"))',label:'functions.py'},
  {note:{en:'A <b>list comprehension</b> builds a list in one line: <code>[expr for x in seq if cond]</code>. A <b>lambda</b> is a tiny anonymous function.',
         de:'Eine <b>List Comprehension</b> baut eine Liste in einer Zeile: <code>[ausdruck for x in seq if bedingung]</code>. Ein <b>Lambda</b> ist eine winzige anonyme Funktion.',
         fa:'<b>List comprehension</b> لیست را در یک خط می‌سازد: <code>[expr for x in seq if cond]</code>. <b>lambda</b> تابع کوچک بی‌نام است.'}},
  {code:'squares = [x * x for x in range(8)]\nevens = [x for x in range(20) if x % 2 == 0]\nprint(squares)\nprint(evens)\n\ndouble = lambda x: 2 * x\nprint(double(21))\nprint(sorted(["banana","fig","apple"], key=len))',label:'comprehensions.py'},
  {note:{en:'<b>Modules</b> extend Python: <code>import math</code> gives you π, square roots and more. This is exactly how NumPy & friends work too.',
         de:'<b>Module</b> erweitern Python: <code>import math</code> liefert π, Wurzeln und mehr. Genau so funktionieren auch NumPy & Co.',
         fa:'<b>ماژول‌ها</b> پایتون را گسترش می‌دهند: <code>import math</code> عدد π، جذر و بیشتر می‌دهد. NumPy و بقیه هم همین‌طورند.'}},
  {code:'import math\n\nprint(math.pi)\nprint(math.sqrt(2))\nprint(math.floor(3.7), math.ceil(3.2))\nprint(round(math.e, 3))',label:'modules.py'},
 ],
 quiz:[
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'def f(x, y=10):\n    return x + y\n\nprint(f(5))',
   opts:['5','15','10','Error'],a:1,
   ex:{en:'y falls back to its default 10 → 5 + 10.',de:'y nimmt den Standardwert 10 → 5 + 10.',fa:'y مقدار پیش‌فرض ۱۰ را می‌گیرد → ۵+۱۰.'}},
  {q:{en:'What does a function without <code>return</code> give back?',de:'Was liefert eine Funktion ohne <code>return</code>?',fa:'تابع بدون <code>return</code> چه برمی‌گرداند؟'},
   opts:['0','""','None','Error'],a:2,
   ex:{en:'No return → the function returns None.',de:'Ohne return → die Funktion liefert None.',fa:'بدون return مقدار None برمی‌گردد.'}},
  {q:{en:'What is <code>[x*x for x in range(4)]</code>?',de:'Was ist <code>[x*x for x in range(4)]</code>?',fa:'<code>[x*x for x in range(4)]</code> چیست؟'},
   opts:['[0, 1, 4, 9]','[1, 4, 9, 16]','[0, 1, 2, 3]','[1, 2, 3, 4]'],a:0,
   ex:{en:'Squares of 0..3: 0, 1, 4, 9.',de:'Quadrate von 0..3: 0, 1, 4, 9.',fa:'مربع ۰ تا ۳: ۰،۱،۴،۹.'}},
  {q:{en:'Which line defines a lambda that adds 1?',de:'Welche Zeile definiert ein Lambda, das 1 addiert?',fa:'کدام خط lambda برای افزودن ۱ است؟'},
   opts:['lambda x: x + 1','def x: x + 1','lambda(x) = x + 1','x => x + 1'],a:0,
   ex:{en:'lambda parameters: expression.',de:'lambda Parameter: Ausdruck.',fa:'ساختار: lambda ورودی: عبارت.'}},
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'nums = [3, 1, 2]\nprint(sorted(nums), nums[0])',
   opts:['[1, 2, 3] 3','[1, 2, 3] 1','[3, 1, 2] 3','Error'],a:0,
   ex:{en:'sorted() returns a NEW list; nums is unchanged.',de:'sorted() liefert eine NEUE Liste; nums bleibt gleich.',fa:'sorted() لیست جدید می‌دهد؛ nums تغییر نمی‌کند.'}},
  {q:{en:'How do you import the math module?',de:'Wie importiert man das math-Modul?',fa:'ماژول math را چگونه وارد می‌کنی؟'},
   opts:['include math','import math','using math','load math'],a:1,
   ex:{en:'<code>import math</code> — then use math.pi etc.',de:'<code>import math</code> — dann math.pi usw.',fa:'<code>import math</code> — سپس math.pi و غیره.'}},
 ]},

/* ================= 5 · NUMPY I ================= */
{ic:'🔢',col:'#4b8bbe',lv:'NUMPY',
 title:{en:'NumPy Arrays',de:'NumPy-Arrays',fa:'آرایه‌های NumPy'},
 desc:{en:'Arrays, arange, slicing & masks',de:'Arrays, arange, Slicing & Masken',fa:'آرایه، arange، برش و ماسک'},
 blocks:[
  {note:{en:'<b>NumPy</b> is THE library for numeric computing. Its <code>ndarray</code> holds numbers of one type and computes <b>element-wise</b> — no loops needed!',
         de:'<b>NumPy</b> ist DIE Bibliothek fürs numerische Rechnen. Das <code>ndarray</code> enthält Zahlen eines Typs und rechnet <b>elementweise</b> — ganz ohne Schleifen!',
         fa:'<b>NumPy</b> کتابخانه اصلی محاسبات عددی است. <code>ndarray</code> اعداد هم‌نوع را نگه می‌دارد و <b>عنصر به عنصر</b> حساب می‌کند — بدون حلقه!'}},
  {code:'import numpy as np\n\na = np.array([1, 2, 3, 4])\nprint(a * 10)        # element-wise!\nprint(a + a)\nprint(a.dtype, a.shape)',label:'arrays.py'},
  {note:{en:'Create ranges with <code>np.arange(start, stop, step)</code> and evenly spaced points with <code>np.linspace(a, b, n)</code>. Reshape to make matrices.',
         de:'Bereiche mit <code>np.arange(start, stop, step)</code>, gleichmäßige Punkte mit <code>np.linspace(a, b, n)</code>. Mit reshape entstehen Matrizen.',
         fa:'با <code>np.arange</code> بازه بساز و با <code>np.linspace(a,b,n)</code> نقاط با فاصله برابر. با reshape ماتریس بساز.'}},
  {code:'import numpy as np\n\nprint(np.arange(0, 10, 2))\nprint(np.linspace(0, 1, 5))\n\nm = np.arange(12).reshape(3, 4)\nprint(m)\nprint("row 1:", m[1])\nprint("col 2:", m[:, 2])',label:'ranges.py'},
  {note:{en:'<b>Boolean masks</b> are NumPy superpowers: compare an array, get True/False, and use it to filter.',
         de:'<b>Boolesche Masken</b> sind NumPys Superkraft: Array vergleichen, True/False bekommen, damit filtern.',
         fa:'<b>ماسک بولی</b> ابرقدرت NumPy است: آرایه را مقایسه کن، True/False بگیر و با آن فیلتر کن.'}},
  {code:'import numpy as np\n\nx = np.array([3, 8, 1, 9, 5, 7])\nmask = x > 4\nprint(mask)\nprint(x[mask])          # filter\nprint(x[x % 2 == 1])    # odd values only',label:'masks.py'},
 ],
 quiz:[
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'import numpy as np\na = np.array([1, 2, 3])\nprint(a * 2)',
   opts:['[1 2 3 1 2 3]','[2 4 6]','[1 4 9]','Error'],a:1,
   ex:{en:'NumPy multiplies element-wise (a Python list would repeat!).',de:'NumPy rechnet elementweise (eine Python-Liste würde wiederholen!).',fa:'NumPy عنصر به عنصر ضرب می‌کند (لیست پایتون تکرار می‌شد!).'}},
  {q:{en:'What is <code>np.arange(0, 10, 3)</code>?',de:'Was ist <code>np.arange(0, 10, 3)</code>?',fa:'<code>np.arange(0,10,3)</code> چیست؟'},
   opts:['[0 3 6 9]','[0 3 6 9 12]','[3 6 9]','[0 10 3]'],a:0,
   ex:{en:'Start 0, step 3, stop before 10.',de:'Start 0, Schritt 3, Stopp vor 10.',fa:'از ۰ با گام ۳ تا قبل از ۱۰.'}},
  {q:{en:'How many values does <code>np.linspace(0, 1, 5)</code> return?',de:'Wie viele Werte liefert <code>np.linspace(0, 1, 5)</code>?',fa:'<code>np.linspace(0,1,5)</code> چند مقدار می‌دهد؟'},
   opts:['4','5','6','10'],a:1,
   ex:{en:'linspace(a, b, n) returns exactly n points, ends included.',de:'linspace(a, b, n) liefert genau n Punkte, Enden inklusive.',fa:'linspace دقیقاً n نقطه می‌دهد، هر دو سر شامل.'}},
  {q:{en:'For a 3×4 matrix m, what is <code>m[:, 0]</code>?',de:'Für eine 3×4-Matrix m: was ist <code>m[:, 0]</code>?',fa:'برای ماتریس ۳×۴، <code>m[:,0]</code> چیست؟'},
   opts:['first row','first column','element (0,0)','last column'],a:1,
   ex:{en:'“:” = all rows, 0 = column 0 → the first column.',de:'„:“ = alle Zeilen, 0 = Spalte 0 → die erste Spalte.',fa:'«:» یعنی همه سطرها و ۰ یعنی ستون اول.'}},
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'import numpy as np\nx = np.array([5, 12, 3, 20])\nprint(x[x > 10])',
   opts:['[12 20]','[5 3]','[True False]','[10 20]'],a:0,
   ex:{en:'The mask keeps values greater than 10.',de:'Die Maske behält Werte über 10.',fa:'ماسک فقط مقادیر بزرگ‌تر از ۱۰ را نگه می‌دارد.'}},
  {q:{en:'What is the shape of <code>np.arange(12).reshape(3, 4)</code>?',de:'Welche Form hat <code>np.arange(12).reshape(3, 4)</code>?',fa:'شکل <code>np.arange(12).reshape(3,4)</code> چیست؟'},
   opts:['(12,)','(4, 3)','(3, 4)','(2, 6)'],a:2,
   ex:{en:'reshape(3, 4) → 3 rows, 4 columns.',de:'reshape(3, 4) → 3 Zeilen, 4 Spalten.',fa:'reshape(3,4) یعنی ۳ سطر و ۴ ستون.'}},
 ]},

/* ================= 6 · NUMPY II & SCIPY ================= */
{ic:'➗',col:'#2b6cb0',lv:'NUMPY',
 title:{en:'NumPy Math & SciPy',de:'NumPy-Mathe & SciPy',fa:'ریاضی NumPy و SciPy'},
 desc:{en:'Statistics, broadcasting, random & SciPy',de:'Statistik, Broadcasting, Zufall & SciPy',fa:'آمار، broadcasting، تصادف و SciPy'},
 blocks:[
  {note:{en:'Arrays know their own <b>statistics</b>: <code>mean, std, min, max, sum</code>. With an <code>axis</code> you aggregate rows or columns of a matrix.',
         de:'Arrays kennen ihre <b>Statistik</b>: <code>mean, std, min, max, sum</code>. Mit <code>axis</code> aggregierst du Zeilen oder Spalten einer Matrix.',
         fa:'آرایه‌ها <b>آمار</b> خودشان را می‌دانند: <code>mean, std, min, max, sum</code>. با <code>axis</code> روی سطر یا ستون ماتریس جمع می‌زنی.'}},
  {code:'import numpy as np\n\ndata = np.array([[4, 9, 2],\n                 [7, 1, 6]])\nprint("mean:", data.mean())\nprint("per column:", data.mean(axis=0))\nprint("per row:   ", data.sum(axis=1))\nprint("max:", data.max(), "std:", round(data.std(), 2))',label:'stats.py'},
  {note:{en:'<b>Broadcasting</b>: NumPy stretches shapes automatically — combine a matrix with a row or a scalar without writing loops.',
         de:'<b>Broadcasting</b>: NumPy dehnt Formen automatisch — kombiniere Matrix mit Zeile oder Skalar ganz ohne Schleifen.',
         fa:'<b>Broadcasting</b>: NumPy شکل‌ها را خودکار هماهنگ می‌کند — ماتریس را با سطر یا عدد ترکیب کن، بدون حلقه.'}},
  {code:'import numpy as np\n\nm = np.zeros((3, 3)) + np.array([1, 2, 3])\nprint(m)\n\nrng = np.random.default_rng(42)\nr = rng.integers(1, 7, size=10)   # dice rolls\nprint(r, "mean:", r.mean())',label:'broadcast.py'},
  {note:{en:'<b>SciPy</b> builds on NumPy: <code>scipy.stats</code> for statistics, <code>scipy.integrate</code> for integrals, <code>scipy.optimize</code> to find minima and roots.',
         de:'<b>SciPy</b> baut auf NumPy auf: <code>scipy.stats</code> für Statistik, <code>scipy.integrate</code> für Integrale, <code>scipy.optimize</code> für Minima und Nullstellen.',
         fa:'<b>SciPy</b> روی NumPy ساخته شده: <code>scipy.stats</code> برای آمار، <code>scipy.integrate</code> برای انتگرال و <code>scipy.optimize</code> برای کمینه و ریشه.'}},
  {code:'from scipy import integrate, optimize\nimport numpy as np\n\n# integral of sin(x) from 0 to pi  (= 2)\narea, err = integrate.quad(np.sin, 0, np.pi)\nprint("area:", round(area, 6))\n\n# minimum of (x-3)^2 + 1\nres = optimize.minimize_scalar(lambda x: (x - 3)**2 + 1)\nprint("minimum at x =", round(res.x, 3))',label:'scipy_intro.py'},
  {code:'from scipy import stats\nimport numpy as np\n\nheights = np.array([170, 165, 180, 175, 168, 172, 177])\nprint("mean:", np.mean(heights))\nprint("median:", np.median(heights))\nz = stats.zscore(heights)\nprint("z-scores:", np.round(z, 2))',label:'scipy_stats.py'},
 ],
 quiz:[
  {q:{en:'What does <code>np.array([[1,2],[3,4]]).mean()</code> give?',de:'Was ergibt <code>np.array([[1,2],[3,4]]).mean()</code>?',fa:'<code>mean()</code> برای [[1,2],[3,4]] چیست؟'},
   opts:['2.5','10','2','[2, 3]'],a:0,
   ex:{en:'(1+2+3+4)/4 = 2.5.',de:'(1+2+3+4)/4 = 2,5.',fa:'(۱+۲+۳+۴)/۴ = ۲٫۵.'}},
  {q:{en:'<code>data.mean(axis=0)</code> averages over…',de:'<code>data.mean(axis=0)</code> mittelt über …',fa:'<code>mean(axis=0)</code> روی چه میانگین می‌گیرد؟'},
   opts:['each row','each column','the whole array','nothing'],a:1,
   ex:{en:'axis=0 collapses the rows → one value per column.',de:'axis=0 kollabiert die Zeilen → ein Wert pro Spalte.',fa:'axis=0 سطرها را جمع می‌کند → یک مقدار برای هر ستون.'}},
  {q:{en:'What shape results from <code>np.zeros((2,3)) + np.array([1,2,3])</code>?',de:'Welche Form hat <code>np.zeros((2,3)) + np.array([1,2,3])</code>?',fa:'شکل حاصل <code>zeros((2,3)) + [1,2,3]</code> چیست؟'},
   opts:['(2, 3)','(3, 2)','(6,)','Error'],a:0,
   ex:{en:'The row [1,2,3] is broadcast onto both rows → (2,3).',de:'Die Zeile [1,2,3] wird auf beide Zeilen gebroadcastet → (2,3).',fa:'سطر [1,2,3] روی هر دو سطر پخش می‌شود → (2,3).'}},
  {q:{en:'Which SciPy module computes integrals?',de:'Welches SciPy-Modul berechnet Integrale?',fa:'کدام ماژول SciPy انتگرال می‌گیرد؟'},
   opts:['scipy.stats','scipy.integrate','scipy.optimize','scipy.linalg'],a:1,
   ex:{en:'scipy.integrate — e.g. quad() for 1-D integrals.',de:'scipy.integrate — z. B. quad() für 1-D-Integrale.',fa:'scipy.integrate — مثلاً quad برای انتگرال یک‌بعدی.'}},
  {q:{en:'<code>integrate.quad(np.sin, 0, np.pi)</code> ≈ ?',de:'<code>integrate.quad(np.sin, 0, np.pi)</code> ≈ ?',fa:'<code>quad(np.sin,0,π)</code> تقریباً چند است؟'},
   opts:['0','1','2','3.14'],a:2,
   ex:{en:'∫ sin(x) dx from 0 to π = 2.',de:'∫ sin(x) dx von 0 bis π = 2.',fa:'انتگرال sin از ۰ تا π برابر ۲ است.'}},
  {q:{en:'Why seed a generator: <code>default_rng(42)</code>?',de:'Warum einen Generator seeden: <code>default_rng(42)</code>?',fa:'چرا <code>default_rng(42)</code> را seed می‌کنیم؟'},
   opts:['faster random numbers','reproducible results','bigger numbers','it is required'],a:1,
   ex:{en:'Same seed → same “random” sequence every run.',de:'Gleicher Seed → gleiche „Zufalls“-Folge bei jedem Lauf.',fa:'seed یکسان → همان دنباله تصادفی در هر اجرا.'}},
 ]},

/* ================= 7 · PANDAS ================= */
{ic:'🐼',col:'#5a4bc0',lv:'DATA',
 title:{en:'Pandas',de:'Pandas',fa:'پانداس'},
 desc:{en:'DataFrames, selection, filtering, groupby',de:'DataFrames, Auswahl, Filter, groupby',fa:'دیتافریم، انتخاب، فیلتر و groupby'},
 blocks:[
  {note:{en:'<b>Pandas</b> is Python\'s spreadsheet. A <b>DataFrame</b> is a table with named columns; a <b>Series</b> is one column.',
         de:'<b>Pandas</b> ist Pythons Tabellenkalkulation. Ein <b>DataFrame</b> ist eine Tabelle mit benannten Spalten; eine <b>Series</b> ist eine Spalte.',
         fa:'<b>پانداس</b> صفحه‌گسترده پایتون است. <b>DataFrame</b> جدولی با ستون‌های نام‌دار و <b>Series</b> یک ستون است.'}},
  {code:'import pandas as pd\n\ndf = pd.DataFrame({\n    "city": ["Berlin", "Munich", "Hamburg", "Cologne"],\n    "pop":  [3.7, 1.5, 1.9, 1.1],      # millions\n    "area": [892, 310, 755, 405],      # km2\n})\nprint(df)\nprint()\nprint(df.describe().round(2))',label:'dataframe.py'},
  {note:{en:'Select a column with <code>df["col"]</code>, rows by position with <code>df.iloc[i]</code>, and filter with a boolean condition — just like NumPy masks.',
         de:'Spalte mit <code>df["col"]</code> wählen, Zeilen per Position mit <code>df.iloc[i]</code>, filtern mit boolescher Bedingung — wie NumPy-Masken.',
         fa:'ستون را با <code>df["col"]</code>، سطر را با <code>df.iloc[i]</code> بگیر و با شرط بولی فیلتر کن — مثل ماسک NumPy.'}},
  {code:'import pandas as pd\ndf = pd.DataFrame({\n    "city": ["Berlin", "Munich", "Hamburg", "Cologne"],\n    "pop":  [3.7, 1.5, 1.9, 1.1],\n})\n\nprint(df["pop"].mean())          # column stats\nprint(df.iloc[0])                # first row\nbig = df[df["pop"] > 1.4]        # filter!\nprint(big)',label:'select.py'},
  {note:{en:'New columns come from calculations on existing ones. <b>groupby</b> splits data into groups and aggregates each — the heart of data analysis.',
         de:'Neue Spalten entstehen aus Berechnungen. <b>groupby</b> teilt Daten in Gruppen und aggregiert sie — das Herz der Datenanalyse.',
         fa:'ستون جدید از محاسبه روی ستون‌های موجود ساخته می‌شود. <b>groupby</b> داده را گروه‌بندی و خلاصه می‌کند — قلب تحلیل داده.'}},
  {code:'import pandas as pd\n\nsales = pd.DataFrame({\n    "fruit": ["apple","apple","banana","banana","cherry"],\n    "qty":   [3, 5, 2, 7, 4],\n    "price": [1.2, 1.1, 0.5, 0.4, 2.5],\n})\nsales["total"] = sales.qty * sales.price\nprint(sales)\nprint()\nprint(sales.groupby("fruit")["total"].sum())',label:'groupby.py'},
 ],
 quiz:[
  {q:{en:'How do you select the column <code>"pop"</code>?',de:'Wie wählt man die Spalte <code>"pop"</code>?',fa:'ستون <code>"pop"</code> را چگونه انتخاب می‌کنی؟'},
   opts:['df.get.pop','df["pop"]','df(pop)','df{"pop"}'],a:1,
   ex:{en:'Square brackets with the column name (df.pop would clash with a method).',de:'Eckige Klammern mit Spaltennamen.',fa:'کروشه با نام ستون.'}},
  {q:{en:'What does <code>df.iloc[0]</code> return?',de:'Was liefert <code>df.iloc[0]</code>?',fa:'<code>df.iloc[0]</code> چه می‌دهد؟'},
   opts:['first column','first row','cell (0,0)','the index'],a:1,
   ex:{en:'iloc selects by position — row 0.',de:'iloc wählt nach Position — Zeile 0.',fa:'iloc بر اساس موقعیت انتخاب می‌کند — سطر ۰.'}},
  {q:{en:'What does <code>df[df["pop"] > 2]</code> do?',de:'Was macht <code>df[df["pop"] > 2]</code>?',fa:'<code>df[df["pop"]>2]</code> چه می‌کند؟'},
   opts:['sorts by pop','keeps rows where pop > 2','deletes the pop column','sets pop to 2'],a:1,
   ex:{en:'Boolean filtering keeps matching rows.',de:'Boolesches Filtern behält passende Zeilen.',fa:'فیلتر بولی فقط سطرهای درست را نگه می‌دارد.'}},
  {q:{en:'Which method shows count / mean / std per column?',de:'Welche Methode zeigt count / mean / std pro Spalte?',fa:'کدام متد count/mean/std هر ستون را می‌دهد؟'},
   opts:['df.info()','df.describe()','df.head()','df.stats()'],a:1,
   ex:{en:'describe() summarises the numeric columns.',de:'describe() fasst numerische Spalten zusammen.',fa:'describe() خلاصه آماری ستون‌های عددی است.'}},
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'import pandas as pd\ns = pd.Series([2, 4, 6])\nprint(s.mean())',
   opts:['4.0','12','6','2'],a:0,
   ex:{en:'(2+4+6)/3 = 4.0.',de:'(2+4+6)/3 = 4,0.',fa:'(۲+۴+۶)/۳ = ۴.'}},
  {q:{en:'<code>sales.groupby("fruit")["qty"].sum()</code> gives…',de:'<code>sales.groupby("fruit")["qty"].sum()</code> liefert …',fa:'<code>groupby("fruit")["qty"].sum()</code> چه می‌دهد؟'},
   opts:['total qty per fruit','the biggest fruit','qty of the first fruit','a sorted table'],a:0,
   ex:{en:'groupby splits per fruit, sum() adds each group\'s qty.',de:'groupby teilt pro Frucht, sum() addiert je Gruppe.',fa:'groupby بر اساس میوه جدا می‌کند و sum جمع هر گروه است.'}},
 ]},

/* ================= 8 · MATPLOTLIB ================= */
{ic:'📊',col:'#c0356b',lv:'PLOTS',
 title:{en:'Matplotlib',de:'Matplotlib',fa:'مت‌پلات‌لیب'},
 desc:{en:'Line, scatter, bar & histogram plots',de:'Linien-, Streu-, Balken- & Histogramm-Plots',fa:'نمودار خطی، پراکندگی، میله‌ای و هیستوگرام'},
 blocks:[
  {note:{en:'<b>Matplotlib</b> draws charts. The pattern is always: prepare data → <code>plt.plot(...)</code> → labels → <code>plt.show()</code>. Run the examples — the figure appears right below!',
         de:'<b>Matplotlib</b> zeichnet Diagramme. Das Muster: Daten vorbereiten → <code>plt.plot(...)</code> → Beschriften → <code>plt.show()</code>. Führ die Beispiele aus — die Grafik erscheint direkt darunter!',
         fa:'<b>مت‌پلات‌لیب</b> نمودار می‌کشد. الگو همیشه: آماده‌سازی داده ← <code>plt.plot</code> ← برچسب‌ها ← <code>plt.show()</code>. اجرا کن — شکل همین پایین ظاهر می‌شود!'}},
  {code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nx = np.linspace(0, 2*np.pi, 200)\nplt.plot(x, np.sin(x), label="sin")\nplt.plot(x, np.cos(x), "--", label="cos")\nplt.title("Sine & Cosine")\nplt.xlabel("x"); plt.ylabel("y")\nplt.legend(); plt.grid(True)\nplt.show()',label:'line.py'},
  {note:{en:'<b>Scatter</b> shows point clouds, <b>bar</b> compares categories, <b>hist</b> shows a distribution.',
         de:'<b>Scatter</b> zeigt Punktwolken, <b>bar</b> vergleicht Kategorien, <b>hist</b> zeigt eine Verteilung.',
         fa:'<b>scatter</b> ابر نقاط، <b>bar</b> مقایسه دسته‌ها و <b>hist</b> توزیع را نشان می‌دهد.'}},
  {code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nrng = np.random.default_rng(1)\nx = rng.normal(size=200)\ny = 0.6*x + rng.normal(scale=0.5, size=200)\n\nplt.scatter(x, y, s=14, alpha=0.7, c=x, cmap="viridis")\nplt.colorbar(label="x value")\nplt.title("Correlated scatter")\nplt.show()',label:'scatter.py'},
  {code:'import matplotlib.pyplot as plt\n\ncities = ["Berlin", "Munich", "Hamburg", "Cologne"]\npop = [3.7, 1.5, 1.9, 1.1]\n\nplt.bar(cities, pop, color=["#4b8bbe","#2bb5a0","#f5b027","#c0356b"])\nplt.ylabel("Population (millions)")\nplt.title("German cities")\nplt.show()',label:'bar.py'},
  {code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nrng = np.random.default_rng(7)\ndata = rng.normal(loc=170, scale=8, size=1000)\n\nplt.hist(data, bins=30, color="#4b8bbe", edgecolor="white")\nplt.title("Heights (simulated)")\nplt.xlabel("cm"); plt.ylabel("count")\nplt.show()',label:'hist.py'},
 ],
 quiz:[
  {q:{en:'Which function draws a line chart?',de:'Welche Funktion zeichnet ein Liniendiagramm?',fa:'کدام تابع نمودار خطی می‌کشد؟'},
   opts:['plt.line()','plt.plot()','plt.draw()','plt.chart()'],a:1,
   ex:{en:'plt.plot(x, y) draws lines.',de:'plt.plot(x, y) zeichnet Linien.',fa:'plt.plot(x,y) خط می‌کشد.'}},
  {q:{en:'Which chart shows a distribution best?',de:'Welches Diagramm zeigt eine Verteilung am besten?',fa:'کدام نمودار توزیع را بهتر نشان می‌دهد؟'},
   opts:['bar','scatter','histogram','pie'],a:2,
   ex:{en:'Histograms bin values and show their frequency.',de:'Histogramme gruppieren Werte und zeigen Häufigkeit.',fa:'هیستوگرام فراوانی مقادیر را نشان می‌دهد.'}},
  {q:{en:'What does <code>plt.legend()</code> need to work?',de:'Was braucht <code>plt.legend()</code>?',fa:'<code>plt.legend()</code> به چه نیاز دارد؟'},
   opts:['a title','label= on the plots','a grid','colors'],a:1,
   ex:{en:'Each plot needs label="…" to appear in the legend.',de:'Jeder Plot braucht label="…" für die Legende.',fa:'هر نمودار باید label داشته باشد تا در legend بیاید.'}},
  {q:{en:'Which call compares categories with rectangles?',de:'Welcher Aufruf vergleicht Kategorien mit Rechtecken?',fa:'کدام دستور دسته‌ها را با مستطیل مقایسه می‌کند؟'},
   opts:['plt.bar()','plt.hist()','plt.scatter()','plt.box()'],a:0,
   ex:{en:'plt.bar(categories, values).',de:'plt.bar(Kategorien, Werte).',fa:'plt.bar(دسته‌ها، مقادیر).'}},
  {q:{en:'What does <code>plt.xlabel("x")</code> do?',de:'Was macht <code>plt.xlabel("x")</code>?',fa:'<code>plt.xlabel("x")</code> چه می‌کند؟'},
   opts:['sets the title','labels the x-axis','plots x','creates a legend'],a:1,
   ex:{en:'It labels the horizontal axis.',de:'Es beschriftet die x-Achse.',fa:'محور افقی را برچسب می‌زند.'}},
  {q:{en:'In <code>plt.hist(data, bins=30)</code>, bins controls…',de:'In <code>plt.hist(data, bins=30)</code> steuert bins …',fa:'در <code>hist(data, bins=30)</code>، bins چیست؟'},
   opts:['the color','the number of bars','the axis length','the font'],a:1,
   ex:{en:'bins = how many intervals the data is split into.',de:'bins = Anzahl der Intervalle.',fa:'bins تعداد بازه‌های تقسیم داده است.'}},
 ]},

/* ================= 9 · TIME SERIES ================= */
{ic:'🕒',col:'#0e87a5',lv:'TIME',
 title:{en:'Time Series',de:'Zeitreihen',fa:'سری‌های زمانی'},
 desc:{en:'Dates, resampling & rolling windows',de:'Datumswerte, Resampling & rollende Fenster',fa:'تاریخ‌ها، resample و پنجره متحرک'},
 blocks:[
  {note:{en:'Pandas has first-class <b>dates</b>: <code>pd.date_range</code> builds a calendar index, and a Series indexed by dates becomes a <b>time series</b>.',
         de:'Pandas kann <b>Datumswerte</b> richtig gut: <code>pd.date_range</code> erzeugt einen Kalenderindex, und eine Series mit Datumsindex wird zur <b>Zeitreihe</b>.',
         fa:'پانداس با <b>تاریخ‌ها</b> عالی کار می‌کند: <code>pd.date_range</code> یک اندیس تقویمی می‌سازد و Series با اندیس تاریخ، <b>سری زمانی</b> می‌شود.'}},
  {code:'import pandas as pd\nimport numpy as np\n\ndays = pd.date_range("2024-01-01", periods=90, freq="D")\nrng = np.random.default_rng(5)\nsales = pd.Series(100 + rng.normal(0, 8, 90).cumsum(), index=days)\n\nprint(sales.head())\nprint("\\nWeekly mean:")\nprint(sales.resample("W").mean().head().round(1))',label:'timeseries.py'},
  {note:{en:'<code>resample("W")</code> groups by calendar period (weeks, months…). A <b>rolling window</b> instead slides over the data: <code>rolling(7).mean()</code> smooths out daily noise.',
         de:'<code>resample("W")</code> gruppiert nach Kalenderperiode (Wochen, Monate …). Ein <b>rollendes Fenster</b> gleitet über die Daten: <code>rolling(7).mean()</code> glättet das Tagesrauschen.',
         fa:'<code>resample("W")</code> بر اساس دوره تقویمی گروه می‌کند (هفته، ماه…). <b>پنجره متحرک</b> روی داده می‌لغزد: <code>rolling(7).mean()</code> نوسان روزانه را هموار می‌کند.'}},
  {code:'import pandas as pd\nimport numpy as np\nimport matplotlib.pyplot as plt\n\ndays = pd.date_range("2024-01-01", periods=120, freq="D")\nrng = np.random.default_rng(3)\nts = pd.Series(50 + rng.normal(0, 5, 120).cumsum(), index=days)\n\nplt.plot(ts.index, ts, alpha=0.4, label="daily")\nplt.plot(ts.index, ts.rolling(7).mean(), lw=2, label="7-day mean")\nplt.legend(); plt.title("Rolling average")\nplt.xticks(rotation=45); plt.tight_layout()\nplt.show()',label:'rolling.py'},
  {note:{en:'Percentage change (<code>pct_change</code>) and shifting (<code>shift</code>) let you compare each day with the previous one — the base of return analysis.',
         de:'Prozentänderung (<code>pct_change</code>) und Verschieben (<code>shift</code>) vergleichen jeden Tag mit dem vorherigen — die Basis der Rendite-Analyse.',
         fa:'تغییر درصدی (<code>pct_change</code>) و شیفت (<code>shift</code>) هر روز را با روز قبل مقایسه می‌کنند — پایه تحلیل بازده.'}},
  {code:'import pandas as pd\nimport numpy as np\n\ndays = pd.date_range("2024-06-01", periods=10, freq="D")\nprice = pd.Series([100,102,101,105,107,104,108,110,109,113.], index=days)\n\nchg = price.pct_change() * 100\nprint(pd.DataFrame({"price": price, "chg %": chg.round(2)}))\nprint("\\nbest day:", chg.idxmax().date(), round(chg.max(), 2), "%")',label:'returns.py'},
 ],
 quiz:[
  {q:{en:'What does <code>pd.date_range("2024-01-01", periods=5, freq="D")</code> create?',de:'Was erzeugt <code>pd.date_range("2024-01-01", periods=5, freq="D")</code>?',fa:'<code>date_range("2024-01-01", periods=5, freq="D")</code> چه می‌سازد؟'},
   opts:['5 daily dates','5 months','1 date','5 random dates'],a:0,
   ex:{en:'periods=5, freq="D" → Jan 1–5, one per day.',de:'periods=5, freq="D" → 1.–5. Januar, täglich.',fa:'۵ تاریخ روزانه از ۱ تا ۵ ژانویه.'}},
  {q:{en:'<code>ts.resample("M").mean()</code> gives…',de:'<code>ts.resample("M").mean()</code> liefert …',fa:'<code>resample("M").mean()</code> چه می‌دهد؟'},
   opts:['minute averages','monthly averages','a rolling mean','sorted values'],a:1,
   ex:{en:'"M" groups by month, then averages each.',de:'"M" gruppiert monatlich und mittelt.',fa:'"M" یعنی گروه‌بندی ماهانه و میانگین هر ماه.'}},
  {q:{en:'What is the difference between resample and rolling?',de:'Was ist der Unterschied zwischen resample und rolling?',fa:'تفاوت resample و rolling چیست؟'},
   opts:['none','resample = calendar buckets, rolling = sliding window','rolling is faster','resample only for plots'],a:1,
   ex:{en:'resample groups by fixed periods; rolling slides a window.',de:'resample: feste Perioden; rolling: gleitendes Fenster.',fa:'resample دوره ثابت؛ rolling پنجره لغزان.'}},
  {q:{en:'<code>rolling(7).mean()</code> smooths a series over…',de:'<code>rolling(7).mean()</code> glättet über …',fa:'<code>rolling(7).mean()</code> روی چند نقطه هموار می‌کند؟'},
   opts:['7 points','7 %','the whole series','7 columns'],a:0,
   ex:{en:'Each value becomes the mean of the last 7 points.',de:'Jeder Wert wird zum Mittel der letzten 7 Punkte.',fa:'هر مقدار میانگین ۷ نقطه اخیر می‌شود.'}},
  {q:{en:'What does <code>price.pct_change()</code> compute?',de:'Was berechnet <code>price.pct_change()</code>?',fa:'<code>pct_change()</code> چه حساب می‌کند؟'},
   opts:['absolute change','percent change vs previous value','cumulative sum','price · 100'],a:1,
   ex:{en:'(today − yesterday) / yesterday.',de:'(heute − gestern) / gestern.',fa:'(امروز − دیروز) ÷ دیروز.'}},
  {q:{en:'The first value of <code>rolling(7).mean()</code> is…',de:'Der erste Wert von <code>rolling(7).mean()</code> ist …',fa:'اولین مقدار <code>rolling(7).mean()</code> چیست؟'},
   opts:['0','NaN (not enough data yet)','the first price','7'],a:1,
   ex:{en:'A 7-window needs 7 values — the first 6 are NaN.',de:'Ein 7er-Fenster braucht 7 Werte — die ersten 6 sind NaN.',fa:'پنجره ۷تایی به ۷ مقدار نیاز دارد — ۶ مقدار اول NaN است.'}},
 ]},

/* ================= 10 · ADVANCED PYTHON ================= */
{ic:'🧠',col:'#7b5fe0',lv:'PRO',
 title:{en:'Advanced Python',de:'Fortgeschrittenes Python',fa:'پایتون پیشرفته'},
 desc:{en:'Classes, exceptions, generators, *args',de:'Klassen, Exceptions, Generatoren, *args',fa:'کلاس‌ها، خطاها، جنریتورها و *args'},
 blocks:[
  {note:{en:'<b>Classes</b> bundle data + behaviour. <code>__init__</code> runs when an object is created; <code>self</code> is the object itself.',
         de:'<b>Klassen</b> bündeln Daten + Verhalten. <code>__init__</code> läuft beim Erzeugen; <code>self</code> ist das Objekt selbst.',
         fa:'<b>کلاس‌ها</b> داده و رفتار را کنار هم می‌گذارند. <code>__init__</code> هنگام ساخت شیء اجرا می‌شود؛ <code>self</code> خود شیء است.'}},
  {code:'class BankAccount:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self\n\n    def __repr__(self):\n        return f"<{self.owner}: {self.balance}€>"\n\nacc = BankAccount("Ada", 100)\nacc.deposit(50).deposit(25)\nprint(acc)',label:'classes.py'},
  {note:{en:'<b>Exceptions</b> handle errors gracefully: <code>try / except / finally</code>. Catch specific errors, not everything.',
         de:'<b>Exceptions</b> behandeln Fehler sauber: <code>try / except / finally</code>. Fange gezielt, nicht alles.',
         fa:'<b>استثناها</b> خطا را تمیز مدیریت می‌کنند: <code>try / except / finally</code>. خطای مشخص را بگیر، نه همه‌چیز را.'}},
  {code:'def safe_div(a, b):\n    try:\n        return a / b\n    except ZeroDivisionError:\n        return float("inf")\n    finally:\n        print(f"  (tried {a}/{b})")\n\nprint(safe_div(10, 2))\nprint(safe_div(5, 0))',label:'exceptions.py'},
  {note:{en:'<b>Generators</b> produce values lazily with <code>yield</code> — perfect for big or infinite sequences.',
         de:'<b>Generatoren</b> erzeugen Werte träge mit <code>yield</code> — perfekt für große oder unendliche Folgen.',
         fa:'<b>جنریتورها</b> با <code>yield</code> مقدارها را تنبل تولید می‌کنند — عالی برای دنباله‌های بزرگ یا بی‌پایان.'}},
  {code:'def fib():\n    a, b = 0, 1\n    while True:\n        yield a\n        a, b = b, a + b\n\ng = fib()\nfirst = [next(g) for _ in range(10)]\nprint(first)\n\nsquares = (x*x for x in range(6))   # generator expression\nprint(sum(squares))',label:'generators.py'},
  {note:{en:'<code>*args</code> collects extra positional arguments, <code>**kwargs</code> extra keyword arguments — and <code>*</code>/<code>**</code> also unpack.',
         de:'<code>*args</code> sammelt zusätzliche Positionsargumente, <code>**kwargs</code> Keyword-Argumente — <code>*</code>/<code>**</code> packen auch aus.',
         fa:'<code>*args</code> آرگومان‌های اضافی و <code>**kwargs</code> آرگومان‌های کلیددار را جمع می‌کند — <code>*</code>/<code>**</code> باز هم می‌کنند.'}},
  {code:'def report(title, *values, **options):\n    print(title.upper() if options.get("upper") else title)\n    for v in values:\n        print(" -", v)\n\nreport("scores", 87, 92, 78, upper=True)\n\nnums = [3, 1, 2]\nprint(max(*nums), sorted(nums))',label:'args.py'},
 ],
 quiz:[
  {q:{en:'When does <code>__init__</code> run?',de:'Wann läuft <code>__init__</code>?',fa:'<code>__init__</code> کی اجرا می‌شود؟'},
   opts:['on import','when an object is created','on every method call','never automatically'],a:1,
   ex:{en:'It initialises each new instance.',de:'Es initialisiert jede neue Instanz.',fa:'هنگام ساخت هر شیء جدید.'}},
  {q:{en:'What is <code>self</code>?',de:'Was ist <code>self</code>?',fa:'<code>self</code> چیست؟'},
   opts:['a keyword','the current object','the class name','a module'],a:1,
   ex:{en:'The instance the method is called on.',de:'Die Instanz, auf der die Methode läuft.',fa:'همان شیئی که متد رویش صدا زده شده.'}},
  {q:{en:'Which block always runs, error or not?',de:'Welcher Block läuft immer, mit oder ohne Fehler?',fa:'کدام بلوک همیشه اجرا می‌شود؟'},
   opts:['try','except','finally','else'],a:2,
   ex:{en:'finally is for cleanup — it always runs.',de:'finally ist fürs Aufräumen — läuft immer.',fa:'finally برای پاکسازی است و همیشه اجرا می‌شود.'}},
  {q:{en:'What does this print?',de:'Was gibt das aus?',fa:'خروجی چیست؟'},code:'def g():\n    yield 1\n    yield 2\n\nit = g()\nprint(next(it), next(it))',
   opts:['1 1','1 2','2 2','Error'],a:1,
   ex:{en:'Each next() resumes at the following yield.',de:'Jedes next() macht beim nächsten yield weiter.',fa:'هر next() تا yield بعدی پیش می‌رود.'}},
  {q:{en:'A function defined with <code>yield</code> returns…',de:'Eine Funktion mit <code>yield</code> liefert …',fa:'تابعی با <code>yield</code> چه برمی‌گرداند؟'},
   opts:['a list','a generator','None','a tuple'],a:1,
   ex:{en:'Calling it creates a lazy generator object.',de:'Der Aufruf erzeugt einen trägen Generator.',fa:'صدا زدنش یک جنریتور تنبل می‌سازد.'}},
  {q:{en:'In <code>def f(*args)</code>, args is a…',de:'In <code>def f(*args)</code> ist args ein …',fa:'در <code>def f(*args)</code>، args چیست؟'},
   opts:['dict','tuple of extra arguments','string','error'],a:1,
   ex:{en:'*args packs positional arguments into a tuple.',de:'*args packt Positionsargumente in ein Tupel.',fa:'*args آرگومان‌ها را در یک تاپل می‌گذارد.'}},
 ]},

/* ================= 11 · ADVANCED PLOTS ================= */
{ic:'📈',col:'#c0356b',lv:'PLOTS',
 title:{en:'Advanced Plots',de:'Fortgeschrittene Plots',fa:'نمودارهای پیشرفته'},
 desc:{en:'fill_between, twin axes, box & style',de:'fill_between, Zwillingsachsen, Box & Stil',fa:'fill_between، محور دوگانه، جعبه‌ای و استایل'},
 blocks:[
  {note:{en:'<code>fill_between</code> shades an area — great for uncertainty bands and highlighting ranges.',
         de:'<code>fill_between</code> füllt eine Fläche — super für Unsicherheitsbänder und Hervorhebungen.',
         fa:'<code>fill_between</code> ناحیه را رنگ می‌کند — عالی برای نوار عدم قطعیت و برجسته‌سازی.'}},
  {code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nx = np.linspace(0, 10, 200)\ny = np.sin(x)\nerr = 0.2 + 0.1*np.cos(x/2)\n\nplt.plot(x, y, color="#c0356b")\nplt.fill_between(x, y-err, y+err, alpha=0.25, color="#c0356b")\nplt.title("Signal with uncertainty band")\nplt.show()',label:'band.py'},
  {note:{en:'<b>Twin axes</b> show two different scales in one chart: <code>ax2 = ax.twinx()</code>.',
         de:'<b>Zwillingsachsen</b> zeigen zwei Skalen in einem Diagramm: <code>ax2 = ax.twinx()</code>.',
         fa:'<b>محور دوگانه</b> دو مقیاس متفاوت را در یک نمودار نشان می‌دهد: <code>ax2 = ax.twinx()</code>.'}},
  {code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nmonths = np.arange(1, 13)\ntemp = 10 + 12*np.sin((months-4)/12*2*np.pi)\nrain = 80 - 40*np.sin((months-4)/12*2*np.pi)\n\nfig, ax = plt.subplots()\nax.plot(months, temp, "o-", color="tab:red", label="temp °C")\nax.set_ylabel("°C", color="tab:red")\nax2 = ax.twinx()\nax2.bar(months, rain, alpha=0.3, color="tab:blue")\nax2.set_ylabel("rain mm", color="tab:blue")\nplt.title("Climate chart")\nplt.show()',label:'twin.py'},
  {note:{en:'<b>Box plots</b> summarise distributions (median, quartiles, outliers); <b>violin plots</b> add the shape.',
         de:'<b>Boxplots</b> fassen Verteilungen zusammen (Median, Quartile, Ausreißer); <b>Violinplots</b> zeigen zusätzlich die Form.',
         fa:'<b>نمودار جعبه‌ای</b> توزیع را خلاصه می‌کند (میانه، چارک، پرت)؛ <b>ویولنی</b> شکل توزیع را هم می‌دهد.'}},
  {code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nrng = np.random.default_rng(9)\ngroups = [rng.normal(60, 8, 200), rng.normal(70, 5, 200), rng.normal(55, 12, 200)]\n\nfig, ax = plt.subplots(1, 2, figsize=(9, 4))\nax[0].boxplot(groups)\nax[0].set_xticks([1, 2, 3], ["A", "B", "C"])\nax[0].set_title("box")\nax[1].violinplot(groups)\nax[1].set_title("violin")\nplt.show()',label:'boxviolin.py'},
  {note:{en:'One line changes the whole look: <code>plt.style.use("ggplot")</code>. Annotate interesting points with <code>plt.annotate</code>.',
         de:'Eine Zeile ändert den ganzen Look: <code>plt.style.use("ggplot")</code>. Markiere Punkte mit <code>plt.annotate</code>.',
         fa:'یک خط همه ظاهر را عوض می‌کند: <code>plt.style.use("ggplot")</code>. نقاط مهم را با <code>plt.annotate</code> علامت بزن.'}},
  {code:'import numpy as np\nimport matplotlib.pyplot as plt\nplt.style.use("ggplot")\n\nx = np.linspace(0, 4*np.pi, 300)\ny = np.sin(x) * np.exp(-x/8)\nplt.plot(x, y)\npeak = x[np.argmax(y)]\nplt.annotate("peak", xy=(peak, y.max()),\n             xytext=(peak+2, y.max()),\n             arrowprops=dict(arrowstyle="->"))\nplt.title("Styled + annotated")\nplt.show()\nplt.style.use("default")',label:'style.py'},
 ],
 quiz:[
  {q:{en:'What is <code>fill_between</code> typically used for?',de:'Wofür nutzt man <code>fill_between</code> typisch?',fa:'<code>fill_between</code> معمولاً برای چیست؟'},
   opts:['3-D bars','shaded bands / areas','pie charts','removing axes'],a:1,
   ex:{en:'It shades the area between two curves.',de:'Es füllt die Fläche zwischen zwei Kurven.',fa:'ناحیه بین دو منحنی را رنگ می‌کند.'}},
  {q:{en:'How do you get a second y-axis?',de:'Wie bekommt man eine zweite y-Achse?',fa:'محور y دوم چگونه ساخته می‌شود؟'},
   opts:['ax.double()','ax.twinx()','plt.second()','ax.y2()'],a:1,
   ex:{en:'twinx() shares x but adds an independent y.',de:'twinx() teilt x, fügt eigenes y hinzu.',fa:'twinx() محور x مشترک و y مستقل می‌دهد.'}},
  {q:{en:'The line inside a box plot box is the…',de:'Die Linie in der Box eines Boxplots ist …',fa:'خط داخل جعبه در نمودار جعبه‌ای چیست؟'},
   opts:['mean','median','mode','maximum'],a:1,
   ex:{en:'The box shows quartiles; the line is the median.',de:'Die Box zeigt Quartile; die Linie den Median.',fa:'جعبه چارک‌هاست و خط میانه است.'}},
  {q:{en:'What does <code>plt.style.use("ggplot")</code> change?',de:'Was ändert <code>plt.style.use("ggplot")</code>?',fa:'<code>style.use("ggplot")</code> چه چیزی را تغییر می‌دهد؟'},
   opts:['the data','colors, grid & overall look','the axes values','the figure size only'],a:1,
   ex:{en:'Styles are preset themes for the whole figure.',de:'Styles sind fertige Themes für die ganze Grafik.',fa:'استایل، تم آماده کل نمودار است.'}},
  {q:{en:'<code>plt.annotate</code> is for…',de:'<code>plt.annotate</code> ist für …',fa:'<code>annotate</code> برای چیست؟'},
   opts:['labelling a point with text/arrow','saving the figure','adding a legend','resizing'],a:0,
   ex:{en:'Text plus optional arrow at a data point.',de:'Text plus optionalem Pfeil an einem Punkt.',fa:'متن و فلش روی یک نقطه داده.'}},
  {q:{en:'<code>alpha=0.3</code> controls…',de:'<code>alpha=0.3</code> steuert …',fa:'<code>alpha=0.3</code> چه چیزی را کنترل می‌کند؟'},
   opts:['line width','transparency','angle','animation speed'],a:1,
   ex:{en:'alpha is opacity: 0 invisible … 1 solid.',de:'alpha ist Deckkraft: 0 unsichtbar … 1 voll.',fa:'alpha شفافیت است: ۰ نامرئی تا ۱ کامل.'}},
 ]},

/* ================= 12 · LINEAR ALGEBRA & FFT ================= */
{ic:'🧮',col:'#306698',lv:'MATH',
 title:{en:'Linear Algebra & FFT',de:'Lineare Algebra & FFT',fa:'جبر خطی و FFT'},
 desc:{en:'Solve systems, eigenvalues, regression, FFT',de:'Gleichungssysteme, Eigenwerte, Regression, FFT',fa:'دستگاه معادلات، مقادیر ویژه، رگرسیون و FFT'},
 blocks:[
  {note:{en:'<code>np.linalg</code> solves real math: systems of equations <code>Ax = b</code>, determinants, inverses, eigenvalues.',
         de:'<code>np.linalg</code> löst echte Mathematik: Gleichungssysteme <code>Ax = b</code>, Determinanten, Inverse, Eigenwerte.',
         fa:'<code>np.linalg</code> ریاضی واقعی حل می‌کند: دستگاه <code>Ax=b</code>، دترمینان، معکوس و مقادیر ویژه.'}},
  {code:'import numpy as np\n\n# 2x + y = 5\n#  x + 3y = 10\nA = np.array([[2., 1.], [1., 3.]])\nb = np.array([5., 10.])\nx = np.linalg.solve(A, b)\nprint("solution x, y =", x)\nprint("check:", A @ x)\n\nprint("det:", np.linalg.det(A))\nw, v = np.linalg.eig(A)\nprint("eigenvalues:", np.round(w, 3))',label:'linalg.py'},
  {note:{en:'<b>Linear regression</b> in one call: <code>np.polyfit(x, y, 1)</code> returns slope and intercept of the best-fit line.',
         de:'<b>Lineare Regression</b> in einem Aufruf: <code>np.polyfit(x, y, 1)</code> liefert Steigung und Achsenabschnitt der Ausgleichsgeraden.',
         fa:'<b>رگرسیون خطی</b> با یک دستور: <code>np.polyfit(x, y, 1)</code> شیب و عرض از مبدأ بهترین خط را می‌دهد.'}},
  {code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nrng = np.random.default_rng(2)\nx = np.linspace(0, 10, 40)\ny = 2.0*x + 5 + rng.normal(0, 3, 40)\n\nslope, intercept = np.polyfit(x, y, 1)\nprint(f"y ≈ {slope:.2f}·x + {intercept:.2f}")\n\nplt.scatter(x, y, s=18, label="data")\nplt.plot(x, slope*x + intercept, "r-", lw=2, label="fit")\nplt.legend(); plt.title("Linear regression")\nplt.show()',label:'regression.py'},
  {note:{en:'The <b>FFT</b> (fast Fourier transform) reveals which frequencies hide inside a signal — one of the most useful tools in science.',
         de:'Die <b>FFT</b> (schnelle Fourier-Transformation) zeigt, welche Frequenzen in einem Signal stecken — eines der nützlichsten Werkzeuge der Wissenschaft.',
         fa:'<b>FFT</b> (تبدیل فوریه سریع) نشان می‌دهد چه فرکانس‌هایی داخل سیگنال پنهان است — از مفیدترین ابزارهای علم.'}},
  {code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nt = np.linspace(0, 1, 500, endpoint=False)\nsig = np.sin(2*np.pi*7*t) + 0.5*np.sin(2*np.pi*30*t)\nsig += np.random.default_rng(0).normal(0, 0.3, t.size)\n\nfreq = np.fft.rfftfreq(t.size, d=t[1]-t[0])\nspec = np.abs(np.fft.rfft(sig)) / t.size * 2\n\nfig, ax = plt.subplots(2, 1, figsize=(7, 5))\nax[0].plot(t, sig, lw=0.8); ax[0].set_title("signal (7 Hz + 30 Hz + noise)")\nax[1].plot(freq, spec);     ax[1].set_title("spectrum — peaks at 7 & 30 Hz")\nax[1].set_xlim(0, 60)\nplt.tight_layout(); plt.show()',label:'fft.py'},
 ],
 quiz:[
  {q:{en:'Which call solves <code>Ax = b</code>?',de:'Welcher Aufruf löst <code>Ax = b</code>?',fa:'کدام دستور <code>Ax=b</code> را حل می‌کند؟'},
   opts:['np.linalg.solve(A, b)','np.solve(b, A)','A / b','np.dot(A, b)'],a:0,
   ex:{en:'linalg.solve is the direct (and stable) way.',de:'linalg.solve ist der direkte (stabile) Weg.',fa:'linalg.solve راه مستقیم و پایدار است.'}},
  {q:{en:'<code>np.polyfit(x, y, 1)</code> fits a…',de:'<code>np.polyfit(x, y, 1)</code> passt eine … an.',fa:'<code>polyfit(x,y,1)</code> چه چیزی برازش می‌کند؟'},
   opts:['circle','straight line','parabola','sine'],a:1,
   ex:{en:'Degree 1 polynomial = a line (slope, intercept).',de:'Polynom Grad 1 = Gerade (Steigung, Abschnitt).',fa:'چندجمله‌ای درجه ۱ یعنی خط.'}},
  {q:{en:'What does the FFT of a signal show?',de:'Was zeigt die FFT eines Signals?',fa:'FFT سیگنال چه چیزی را نشان می‌دهد؟'},
   opts:['its future values','its frequency content','its derivative','its average'],a:1,
   ex:{en:'It decomposes the signal into frequencies.',de:'Sie zerlegt das Signal in Frequenzen.',fa:'سیگنال را به فرکانس‌ها تجزیه می‌کند.'}},
  {q:{en:'In NumPy, <code>A @ B</code> is…',de:'In NumPy ist <code>A @ B</code> …',fa:'در NumPy عملگر <code>@</code> چیست؟'},
   opts:['element-wise product','matrix multiplication','division','concatenation'],a:1,
   ex:{en:'@ is the matrix-multiplication operator.',de:'@ ist Matrixmultiplikation.',fa:'@ ضرب ماتریسی است.'}},
  {q:{en:'A matrix is NOT invertible when its determinant is…',de:'Eine Matrix ist NICHT invertierbar, wenn ihre Determinante … ist.',fa:'ماتریس کی معکوس‌پذیر نیست؟'},
   opts:['1','negative','0','very large'],a:2,
   ex:{en:'det = 0 → singular matrix, no inverse.',de:'det = 0 → singulär, keine Inverse.',fa:'دترمینان صفر یعنی ماتریس منفرد.'}},
  {q:{en:'If a signal contains sin(2π·7·t), its spectrum peaks at…',de:'Enthält ein Signal sin(2π·7·t), hat das Spektrum einen Peak bei …',fa:'اگر سیگنال sin(2π·7·t) داشته باشد، طیف کجا قله دارد؟'},
   opts:['7 Hz','2π Hz','0.7 Hz','49 Hz'],a:0,
   ex:{en:'The 7 in 2π·7·t is the frequency in Hz.',de:'Die 7 in 2π·7·t ist die Frequenz in Hz.',fa:'عدد ۷ همان فرکانس بر حسب هرتز است.'}},
 ]},

/* ================= 13 · STATISTICS ================= */
{ic:'🎲',col:'#2b8a78',lv:'STATS',
 title:{en:'Statistics & Probability',de:'Statistik & Wahrscheinlichkeit',fa:'آمار و احتمال'},
 desc:{en:'Distributions, CLT, t-test, correlation',de:'Verteilungen, ZGS, t-Test, Korrelation',fa:'توزیع‌ها، قضیه حد مرکزی، آزمون t و همبستگی'},
 blocks:[
  {note:{en:'<code>scipy.stats</code> models <b>distributions</b>: pdf (density), cdf (cumulative) and random sampling for norm, binom, poisson…',
         de:'<code>scipy.stats</code> modelliert <b>Verteilungen</b>: pdf (Dichte), cdf (kumulativ) und Zufallsziehungen für norm, binom, poisson …',
         fa:'<code>scipy.stats</code> توزیع‌ها را مدل می‌کند: pdf (چگالی)، cdf (تجمعی) و نمونه‌گیری تصادفی.'}},
  {code:'import numpy as np\nimport matplotlib.pyplot as plt\nfrom scipy import stats\n\nx = np.linspace(-4, 4, 300)\nplt.plot(x, stats.norm.pdf(x), label="σ=1")\nplt.plot(x, stats.norm.pdf(x, scale=2), label="σ=2")\nplt.fill_between(x, stats.norm.pdf(x), where=np.abs(x)<1, alpha=0.25)\nplt.title("Normal distribution — ±1σ ≈ 68%")\nplt.legend(); plt.show()\n\nprint("P(x < 1.96):", round(stats.norm.cdf(1.96), 4))',label:'normal.py'},
  {note:{en:'<b>Central Limit Theorem</b>: means of many samples are normally distributed — even if the data itself is not! Watch it happen:',
         de:'<b>Zentraler Grenzwertsatz</b>: Mittelwerte vieler Stichproben sind normalverteilt — selbst wenn die Daten es nicht sind! Sieh selbst:',
         fa:'<b>قضیه حد مرکزی</b>: میانگینِ نمونه‌های زیاد نرمال می‌شود — حتی اگر خود داده نرمال نباشد! ببین:'}},
  {code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nrng = np.random.default_rng(0)\nuniform = rng.random(10_000)                 # flat!\nmeans = rng.random((10_000, 30)).mean(axis=1)  # means of 30\n\nfig, ax = plt.subplots(1, 2, figsize=(9, 3.5))\nax[0].hist(uniform, bins=40); ax[0].set_title("raw uniform data")\nax[1].hist(means, bins=40, color="#2b8a78"); ax[1].set_title("means of 30 → bell!")\nplt.tight_layout(); plt.show()',label:'clt.py'},
  {note:{en:'A <b>t-test</b> asks: could the difference between two groups be pure chance? Small p-value (< 0.05) → probably a real difference.',
         de:'Ein <b>t-Test</b> fragt: Könnte der Unterschied zweier Gruppen Zufall sein? Kleiner p-Wert (< 0,05) → wohl echter Unterschied.',
         fa:'<b>آزمون t</b> می‌پرسد: آیا تفاوت دو گروه شانسی است؟ p کوچک (<۰٫۰۵) یعنی تفاوت احتمالاً واقعی است.'}},
  {code:'import numpy as np\nfrom scipy import stats\n\nrng = np.random.default_rng(4)\nold_design = rng.normal(3.2, 1.0, 80)   # conversion %\nnew_design = rng.normal(3.7, 1.0, 80)\n\nt, p = stats.ttest_ind(new_design, old_design)\nprint(f"means: old {old_design.mean():.2f}  new {new_design.mean():.2f}")\nprint(f"t = {t:.2f},  p = {p:.4f}")\nprint("significant!" if p < 0.05 else "could be chance")',label:'ttest.py'},
  {code:'import numpy as np\n\nrng = np.random.default_rng(8)\nstudy_hours = rng.uniform(0, 10, 100)\nscore = 40 + 5*study_hours + rng.normal(0, 8, 100)\n\nr = np.corrcoef(study_hours, score)[0, 1]\nprint("correlation r =", round(r, 3))\nprint("r² =", round(r*r, 3), "→", round(100*r*r), "% of variance explained")',label:'correlation.py'},
 ],
 quiz:[
  {q:{en:'For a normal distribution, ±1σ around the mean covers about…',de:'Bei Normalverteilung deckt ±1σ um den Mittelwert etwa … ab.',fa:'در توزیع نرمال، ±۱σ حدوداً چند درصد را می‌پوشاند؟'},
   opts:['50 %','68 %','95 %','99 %'],a:1,
   ex:{en:'The 68-95-99.7 rule.',de:'Die 68-95-99,7-Regel.',fa:'قانون ۶۸-۹۵-۹۹.۷.'}},
  {q:{en:'The Central Limit Theorem says sample MEANS become…',de:'Der ZGS sagt: Stichproben-MITTELWERTE werden …',fa:'قضیه حد مرکزی می‌گوید میانگین نمونه‌ها چه می‌شود؟'},
   opts:['uniform','normally distributed','constant','skewed'],a:1,
   ex:{en:'Means approach a normal distribution as n grows.',de:'Mittelwerte nähern sich der Normalverteilung.',fa:'میانگین‌ها به توزیع نرمال میل می‌کنند.'}},
  {q:{en:'A p-value of 0.01 in a t-test means…',de:'Ein p-Wert von 0,01 im t-Test bedeutet …',fa:'p برابر ۰٫۰۱ در آزمون t یعنی چه؟'},
   opts:['1 % chance the difference is this big by luck','the groups are equal','the test failed','99 % correlation'],a:0,
   ex:{en:'Small p → unlikely to be pure chance.',de:'Kleines p → kaum reiner Zufall.',fa:'p کوچک یعنی شانسی بودن بعید است.'}},
  {q:{en:'Which r shows the STRONGEST relationship?',de:'Welches r zeigt den STÄRKSTEN Zusammenhang?',fa:'کدام r قوی‌ترین رابطه است؟'},
   opts:['r = 0.2','r = −0.9','r = 0.5','r = 0'],a:1,
   ex:{en:'Strength is |r| — the sign is only the direction.',de:'Stärke ist |r| — das Vorzeichen ist nur die Richtung.',fa:'قدرت با |r| است؛ علامت فقط جهت است.'}},
  {q:{en:'<code>stats.norm.cdf(0)</code> equals…',de:'<code>stats.norm.cdf(0)</code> ist …',fa:'<code>norm.cdf(0)</code> چند است؟'},
   opts:['0','0.5','1','0.68'],a:1,
   ex:{en:'Half the distribution lies below the mean.',de:'Die Hälfte liegt unter dem Mittelwert.',fa:'نیمی از توزیع زیر میانگین است.'}},
  {q:{en:'r² = 0.64 means the model explains…',de:'r² = 0,64 heißt: das Modell erklärt …',fa:'r²=۰٫۶۴ یعنی مدل چند درصد واریانس را توضیح می‌دهد؟'},
   opts:['64 % of the variance','36 % of the data','0.64 data points','nothing'],a:0,
   ex:{en:'r² is the share of variance explained.',de:'r² ist der erklärte Varianzanteil.',fa:'r² سهم واریانس توضیح‌داده‌شده است.'}},
 ]},

/* ================= 14 · CAPSTONE ================= */
{ic:'🏆',col:'#f0791a',lv:'FINAL',
 title:{en:'Capstone Project',de:'Abschlussprojekt',fa:'پروژه پایانی'},
 desc:{en:'A full mini data analysis, end to end',de:'Eine komplette Mini-Datenanalyse',fa:'یک تحلیل داده کامل از صفر تا صد'},
 blocks:[
  {note:{en:'<b>The mission:</b> analyse a year of shop sales — load, clean, aggregate, visualise, conclude. Everything you learned, in one flow.',
         de:'<b>Die Mission:</b> ein Jahr Verkaufsdaten analysieren — laden, bereinigen, aggregieren, visualisieren, folgern. Alles Gelernte in einem Fluss.',
         fa:'<b>مأموریت:</b> تحلیل یک سال فروش فروشگاه — بارگیری، پاکسازی، تجمیع، نمودار و نتیجه. همه آموخته‌ها در یک جریان.'}},
  {code:'import pandas as pd\nimport numpy as np\n\n# 1) "load" data (here: generate it)\nrng = np.random.default_rng(7)\ndays = pd.date_range("2024-01-01", periods=365)\ntrend = np.linspace(100, 140, 365)\nseason = 15*np.sin(np.arange(365)/365*2*np.pi*4)\nsales = pd.DataFrame({\n    "date": days,\n    "revenue": trend + season + rng.normal(0, 10, 365),\n    "store": rng.choice(["North", "South"], 365),\n})\nsales.loc[rng.choice(365, 8, replace=False), "revenue"] = np.nan  # messy!\nprint(sales.head())\nprint("\\nmissing:", sales.revenue.isna().sum())',label:'step1_load.py'},
  {code:'import pandas as pd\nimport numpy as np\n\nrng = np.random.default_rng(7)\ndays = pd.date_range("2024-01-01", periods=365)\ntrend = np.linspace(100, 140, 365)\nseason = 15*np.sin(np.arange(365)/365*2*np.pi*4)\nsales = pd.DataFrame({"date": days,\n    "revenue": trend + season + rng.normal(0, 10, 365),\n    "store": rng.choice(["North", "South"], 365)})\n\n# 2) clean + 3) aggregate\nsales["revenue"] = sales["revenue"].fillna(sales["revenue"].mean())\nsales["month"] = sales.date.dt.month\nmonthly = sales.groupby("month")["revenue"].mean()\nby_store = sales.groupby("store")["revenue"].agg(["mean", "std"])\nprint(monthly.round(1))\nprint()\nprint(by_store.round(2))',label:'step2_clean.py'},
  {code:'import pandas as pd\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nrng = np.random.default_rng(7)\ndays = pd.date_range("2024-01-01", periods=365)\ntrend = np.linspace(100, 140, 365)\nseason = 15*np.sin(np.arange(365)/365*2*np.pi*4)\nrev = trend + season + rng.normal(0, 10, 365)\nts = pd.Series(rev, index=days)\n\n# 4) dashboard\nfig, ax = plt.subplots(2, 1, figsize=(8, 6))\nax[0].plot(ts.index, ts, alpha=0.35, label="daily")\nax[0].plot(ts.index, ts.rolling(30).mean(), lw=2.5, label="30-day trend")\nax[0].legend(); ax[0].set_title("Revenue 2024 — trend is UP 📈")\nmonthly = ts.resample("ME").mean()\nax[1].bar(monthly.index.strftime("%b"), monthly.values, color="#f0791a")\nax[1].set_title("Monthly averages")\nplt.tight_layout(); plt.show()\nprint("Conclusion: revenue grew ~", round(ts[-30:].mean() - ts[:30].mean()), "per day on average.")',label:'step3_visualize.py'},
  {note:{en:'🎓 <b>You did it!</b> This load → clean → aggregate → visualise → conclude loop is exactly how real data analysis works. Try changing the seasonality or the trend!',
         de:'🎓 <b>Geschafft!</b> Dieser Ablauf laden → bereinigen → aggregieren → visualisieren → folgern ist echte Datenanalyse. Ändere Saisonalität oder Trend!',
         fa:'🎓 <b>موفق شدی!</b> همین چرخه بارگیری ← پاکسازی ← تجمیع ← نمودار ← نتیجه، تحلیل داده واقعی است. فصلی‌بودن یا روند را تغییر بده!'}},
 ],
 quiz:[
  {q:{en:'What is the right FIRST step with a new dataset?',de:'Was ist der richtige ERSTE Schritt mit neuen Daten?',fa:'اولین قدم با داده جدید چیست؟'},
   opts:['plot everything','look at head() / missing values','delete outliers','fit a model'],a:1,
   ex:{en:'Inspect first: shape, head, missing values.',de:'Erst inspizieren: shape, head, fehlende Werte.',fa:'اول بررسی: head و مقادیر گمشده.'}},
  {q:{en:'<code>df["x"].fillna(df["x"].mean())</code> does what?',de:'Was macht <code>df["x"].fillna(df["x"].mean())</code>?',fa:'<code>fillna(mean)</code> چه می‌کند؟'},
   opts:['drops missing rows','replaces NaN with the column mean','sorts x','removes the column'],a:1,
   ex:{en:'A simple imputation strategy.',de:'Eine einfache Imputation.',fa:'جایگزینی ساده مقادیر گمشده.'}},
  {q:{en:'<code>sales.date.dt.month</code> extracts…',de:'<code>sales.date.dt.month</code> extrahiert …',fa:'<code>dt.month</code> چه چیزی می‌دهد؟'},
   opts:['the month of each date','30 days','a month of data','the year'],a:0,
   ex:{en:'.dt gives datetime parts of a column.',de:'.dt liefert Datumsteile einer Spalte.',fa:'.dt اجزای تاریخ ستون را می‌دهد.'}},
  {q:{en:'A 30-day rolling mean on daily data reveals…',de:'Ein 30-Tage-Rolling-Mean auf Tagesdaten zeigt …',fa:'میانگین متحرک ۳۰روزه چه چیزی را آشکار می‌کند؟'},
   opts:['daily noise','the long-term trend','outliers only','nothing'],a:1,
   ex:{en:'Smoothing removes noise, exposing the trend.',de:'Glätten entfernt Rauschen und zeigt den Trend.',fa:'هموارسازی نویز را حذف و روند را نمایان می‌کند.'}},
  {q:{en:'Which chart best compares monthly averages?',de:'Welches Diagramm vergleicht Monatsmittel am besten?',fa:'کدام نمودار میانگین ماهانه را بهتر مقایسه می‌کند؟'},
   opts:['scatter','bar','pie','violin'],a:1,
   ex:{en:'Categories (months) + values → bar chart.',de:'Kategorien (Monate) + Werte → Balken.',fa:'دسته (ماه) و مقدار یعنی نمودار میله‌ای.'}},
  {q:{en:'groupby("store")["revenue"].agg(["mean","std"]) returns…',de:'groupby("store")["revenue"].agg(["mean","std"]) liefert …',fa:'agg(["mean","std"]) چه می‌دهد؟'},
   opts:['one number','mean & std per store','sorted revenue','a plot'],a:1,
   ex:{en:'agg computes several statistics per group.',de:'agg berechnet mehrere Statistiken je Gruppe.',fa:'agg چند آماره برای هر گروه حساب می‌کند.'}},
 ]},
];

/* ================= playground quick snippets ================= */
const SNIPPETS=[
 {t:'Hello',code:'print("Hello, Python! 🐍")\nfor i in range(3):\n    print("run", i)'},
 {t:'NumPy',code:'import numpy as np\na = np.arange(10)\nprint(a)\nprint("sum:", a.sum(), "mean:", a.mean())'},
 {t:'Plot',code:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.linspace(0, 10, 300)\nplt.plot(x, np.sin(x) * np.exp(-x/5))\nplt.title("Damped sine")\nplt.show()'},
 {t:'Pandas',code:'import pandas as pd\ndf = pd.DataFrame({"a":[1,2,3],"b":[10,20,30]})\nprint(df)\nprint(df.describe())'},
 {t:'SciPy',code:'from scipy import optimize\nres = optimize.minimize_scalar(lambda x: (x-2)**2 + 1)\nprint("min at x =", round(res.x, 4))'},
];

/* ================= examples library ================= */
const EXAMPLES=[
 {cat:'Python',icon:'🐍',title:{en:'FizzBuzz',de:'FizzBuzz',fa:'فیزباز'},
  code:'for i in range(1, 21):\n    if i % 15 == 0:   print("FizzBuzz")\n    elif i % 3 == 0:  print("Fizz")\n    elif i % 5 == 0:  print("Buzz")\n    else:             print(i)'},
 {cat:'Python',icon:'🔢',title:{en:'Fibonacci numbers',de:'Fibonacci-Zahlen',fa:'اعداد فیبوناچی'},
  code:'a, b = 0, 1\nfor _ in range(12):\n    print(a, end=" ")\n    a, b = b, a + b\nprint()'},
 {cat:'Python',icon:'🔤',title:{en:'Word frequency counter',de:'Worthäufigkeit zählen',fa:'شمارش تکرار کلمات'},
  code:'text = "the quick brown fox jumps over the lazy dog the fox"\ncounts = {}\nfor w in text.split():\n    counts[w] = counts.get(w, 0) + 1\nfor w, c in sorted(counts.items(), key=lambda kv: -kv[1]):\n    print(f"{w:6s} {c}")'},
 {cat:'Python',icon:'🎲',title:{en:'Guess-my-number logic',de:'Zahlenraten-Logik',fa:'منطق حدس عدد'},
  code:'secret = 37\nlow, high, tries = 1, 100, 0\nwhile True:\n    guess = (low + high) // 2\n    tries += 1\n    print("guess:", guess)\n    if guess == secret:\n        print("found in", tries, "tries!"); break\n    if guess < secret: low = guess + 1\n    else: high = guess - 1'},
 {cat:'Python',icon:'📝',title:{en:'Prime numbers',de:'Primzahlen',fa:'اعداد اول'},
  code:'def is_prime(n):\n    if n < 2: return False\n    for d in range(2, int(n**0.5) + 1):\n        if n % d == 0: return False\n    return True\n\nprimes = [n for n in range(2, 60) if is_prime(n)]\nprint(primes)'},
 {cat:'NumPy',icon:'🎯',title:{en:'Estimate π with random points',de:'π mit Zufallspunkten schätzen',fa:'تخمین π با نقاط تصادفی'},
  code:'import numpy as np\nrng = np.random.default_rng(0)\nN = 200_000\nx, y = rng.random(N), rng.random(N)\ninside = (x**2 + y**2 <= 1).sum()\nprint("π ≈", 4 * inside / N)'},
 {cat:'NumPy',icon:'🧮',title:{en:'Matrix multiplication',de:'Matrixmultiplikation',fa:'ضرب ماتریس'},
  code:'import numpy as np\nA = np.array([[1, 2], [3, 4]])\nB = np.array([[5, 6], [7, 8]])\nprint(A @ B)          # matrix product\nprint(np.linalg.det(A))  # determinant'},
 {cat:'NumPy',icon:'🌡️',title:{en:'Normalize data (z-score)',de:'Daten normalisieren (z-Score)',fa:'نرمال‌سازی داده (z-score)'},
  code:'import numpy as np\nx = np.array([12., 15., 9., 20., 14., 11.])\nz = (x - x.mean()) / x.std()\nprint(np.round(z, 2))\nprint("mean ≈", round(z.mean(), 6), "std ≈", round(z.std(), 6))'},
 {cat:'SciPy',icon:'📐',title:{en:'Solve an equation (root finding)',de:'Gleichung lösen (Nullstelle)',fa:'حل معادله (ریشه‌یابی)'},
  code:'from scipy import optimize\nimport numpy as np\n\n# solve cos(x) = x\nroot = optimize.brentq(lambda x: np.cos(x) - x, 0, 1)\nprint("x =", round(root, 6))'},
 {cat:'SciPy',icon:'🎢',title:{en:'Curve fitting',de:'Kurvenanpassung',fa:'برازش منحنی'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\nfrom scipy.optimize import curve_fit\n\nrng = np.random.default_rng(3)\nx = np.linspace(0, 4, 40)\ny = 2.5 * np.exp(-1.3 * x) + rng.normal(scale=0.1, size=40)\n\nf = lambda x, a, b: a * np.exp(-b * x)\n(a, b), _ = curve_fit(f, x, y)\nprint(f"fit: a={a:.2f}, b={b:.2f}")\n\nplt.scatter(x, y, s=14, label="data")\nplt.plot(x, f(x, a, b), "r-", label="fit")\nplt.legend(); plt.show()'},
 {cat:'SciPy',icon:'∫',title:{en:'Numeric integration',de:'Numerische Integration',fa:'انتگرال عددی'},
  code:'from scipy import integrate\nimport numpy as np\n\nf = lambda x: np.exp(-x**2)\narea, err = integrate.quad(f, -np.inf, np.inf)\nprint("Gauss integral:", round(area, 6))\nprint("sqrt(pi)      :", round(np.sqrt(np.pi), 6))'},
 {cat:'Pandas',icon:'🏙️',title:{en:'City table: filter & sort',de:'Städtetabelle: filtern & sortieren',fa:'جدول شهرها: فیلتر و مرتب‌سازی'},
  code:'import pandas as pd\ndf = pd.DataFrame({\n    "city": ["Berlin","Munich","Hamburg","Cologne","Frankfurt"],\n    "pop":  [3.7, 1.5, 1.9, 1.1, 0.76],\n    "state":["BE","BY","HH","NW","HE"],\n})\nprint(df.sort_values("pop", ascending=False))\nprint()\nprint(df[df["pop"] > 1.2][["city","pop"]])'},
 {cat:'Pandas',icon:'🛒',title:{en:'Sales analysis with groupby',de:'Verkaufsanalyse mit groupby',fa:'تحلیل فروش با groupby'},
  code:'import pandas as pd\nsales = pd.DataFrame({\n    "month":  ["Jan","Jan","Feb","Feb","Mar","Mar"],\n    "product":["A","B","A","B","A","B"],\n    "revenue":[100, 150, 120, 130, 160, 170],\n})\nprint(sales.groupby("month")["revenue"].sum())\nprint()\nprint(sales.pivot_table(index="month", columns="product", values="revenue"))'},
 {cat:'Pandas',icon:'🧹',title:{en:'Missing values',de:'Fehlende Werte',fa:'مقادیر گمشده'},
  code:'import pandas as pd\nimport numpy as np\ndf = pd.DataFrame({"a":[1, np.nan, 3, 4], "b":[10, 20, np.nan, 40]})\nprint(df)\nprint()\nprint("nulls:\\n", df.isna().sum())\nprint()\nprint(df.fillna(df.mean()))'},
 {cat:'Pandas',icon:'📈',title:{en:'DataFrame → chart',de:'DataFrame → Diagramm',fa:'دیتافریم ← نمودار'},
  code:'import pandas as pd\nimport matplotlib.pyplot as plt\ndf = pd.DataFrame({\n    "year": [2019, 2020, 2021, 2022, 2023],\n    "users":[120, 180, 260, 410, 640],\n})\nplt.plot(df.year, df.users, "o-", color="#4b8bbe")\nplt.title("User growth")\nplt.xlabel("year"); plt.ylabel("users (k)")\nplt.grid(True); plt.show()'},
 {cat:'Matplotlib',icon:'🌀',title:{en:'Spiral (polar plot)',de:'Spirale (Polarplot)',fa:'مارپیچ (نمودار قطبی)'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\ntheta = np.linspace(0, 6*np.pi, 500)\nr = theta\nax = plt.subplot(projection="polar")\nax.plot(theta, r, color="#c0356b")\nplt.title("Archimedean spiral")\nplt.show()'},
 {cat:'Matplotlib',icon:'🚶',title:{en:'Random walk',de:'Zufallsbewegung',fa:'گام تصادفی'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\nrng = np.random.default_rng(11)\nsteps = rng.choice([-1, 1], size=1000)\nwalk = steps.cumsum()\nplt.plot(walk, lw=1)\nplt.title("1-D random walk")\nplt.show()'},
 {cat:'Matplotlib',icon:'🗺️',title:{en:'Heatmap of a function',de:'Heatmap einer Funktion',fa:'نقشه حرارتی یک تابع'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.linspace(-3, 3, 200)\nX, Y = np.meshgrid(x, x)\nZ = np.sin(X*Y)\nplt.imshow(Z, extent=[-3,3,-3,3], cmap="magma", origin="lower")\nplt.colorbar(); plt.title("sin(x·y)")\nplt.show()'},
 {cat:'Matplotlib',icon:'🎛️',title:{en:'Subplots (2×2 dashboard)',de:'Subplots (2×2-Dashboard)',fa:'زیرنمودارها (داشبورد ۲×۲)'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.linspace(0, 2*np.pi, 200)\nfig, ax = plt.subplots(2, 2, figsize=(8, 6))\nax[0,0].plot(x, np.sin(x));  ax[0,0].set_title("sin")\nax[0,1].plot(x, np.cos(x), "r"); ax[0,1].set_title("cos")\nax[1,0].hist(np.random.default_rng(0).normal(size=500), bins=25)\nax[1,0].set_title("normal dist")\nax[1,1].scatter(np.random.rand(80), np.random.rand(80))\nax[1,1].set_title("random scatter")\nplt.tight_layout(); plt.show()'},
 {cat:'Matplotlib',icon:'🥧',title:{en:'Pie chart',de:'Kreisdiagramm',fa:'نمودار دایره‌ای'},
  code:'import matplotlib.pyplot as plt\nsizes = [45, 25, 20, 10]\nlabels = ["Python", "C++", "Rust", "other"]\nplt.pie(sizes, labels=labels, autopct="%1.0f%%",\n        colors=["#4b8bbe","#2bb5a0","#f5b027","#c0356b"])\nplt.title("Favourite languages")\nplt.show()'},
];

/* ================= v2: more snippets & advanced examples ================= */
SNIPPETS.push(
 {t:'Time series',code:'import pandas as pd\nimport numpy as np\nimport matplotlib.pyplot as plt\ndays = pd.date_range("2024-01-01", periods=100)\nts = pd.Series(np.random.default_rng(1).normal(0,1,100).cumsum(), index=days)\nplt.plot(ts.index, ts, alpha=.4)\nplt.plot(ts.index, ts.rolling(10).mean(), lw=2)\nplt.xticks(rotation=45); plt.tight_layout(); plt.show()'},
 {t:'FFT',code:'import numpy as np\nimport matplotlib.pyplot as plt\nt = np.linspace(0, 1, 400, endpoint=False)\nsig = np.sin(2*np.pi*5*t) + 0.4*np.sin(2*np.pi*40*t)\nfreq = np.fft.rfftfreq(t.size, t[1]-t[0])\nplt.plot(freq, np.abs(np.fft.rfft(sig)))\nplt.xlim(0, 60); plt.title("spectrum"); plt.show()'},
 {t:'3D',code:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.linspace(-3, 3, 60)\nX, Y = np.meshgrid(x, x)\nZ = np.sin(np.sqrt(X**2 + Y**2))\nax = plt.axes(projection="3d")\nax.plot_surface(X, Y, Z, cmap="viridis")\nplt.title("3-D surface"); plt.show()'},
 {t:'Regression',code:'import numpy as np\nimport matplotlib.pyplot as plt\nrng = np.random.default_rng(0)\nx = np.linspace(0, 10, 50)\ny = 3*x + 7 + rng.normal(0, 4, 50)\nm, c = np.polyfit(x, y, 1)\nplt.scatter(x, y, s=12)\nplt.plot(x, m*x + c, "r")\nplt.title(f"y = {m:.2f}x + {c:.2f}"); plt.show()'}
);

EXAMPLES.push(
 {cat:'Time series',icon:'📉',title:{en:'Stock-style cumulative returns',de:'Kumulierte Renditen (Aktien-Stil)',fa:'بازده تجمعی (مثل سهام)'},
  code:'import pandas as pd\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nrng = np.random.default_rng(21)\ndays = pd.date_range("2024-01-01", periods=252, freq="B")\nfor name, drift in [("Fund A", .0006), ("Fund B", .0002)]:\n    ret = rng.normal(drift, 0.012, len(days))\n    price = 100 * (1 + pd.Series(ret, index=days)).cumprod()\n    plt.plot(price.index, price, label=name)\nplt.legend(); plt.title("One year, two funds")\nplt.xticks(rotation=45); plt.tight_layout(); plt.show()'},
 {cat:'Time series',icon:'🌊',title:{en:'Seasonality + trend decomposition',de:'Saisonalität + Trend zerlegen',fa:'تجزیه روند و فصلی‌بودن'},
  code:'import pandas as pd\nimport numpy as np\nimport matplotlib.pyplot as plt\n\ndays = pd.date_range("2023-01-01", periods=730)\nn = np.arange(730)\nseries = 0.05*n + 8*np.sin(n/365*2*np.pi) + np.random.default_rng(2).normal(0, 2, 730)\nts = pd.Series(series, index=days)\ntrend = ts.rolling(90, center=True).mean()\nseasonal = (ts - trend).rolling(15, center=True).mean()\n\nfig, ax = plt.subplots(3, 1, figsize=(8, 6), sharex=True)\nax[0].plot(ts.index, ts, lw=.6);      ax[0].set_title("raw")\nax[1].plot(trend.index, trend);       ax[1].set_title("trend (90-day)")\nax[2].plot(seasonal.index, seasonal); ax[2].set_title("seasonal wiggle")\nplt.tight_layout(); plt.show()'},
 {cat:'Time series',icon:'📅',title:{en:'Weekday vs weekend pattern',de:'Wochentag vs. Wochenende',fa:'الگوی روز هفته و آخر هفته'},
  code:'import pandas as pd\nimport numpy as np\n\nrng = np.random.default_rng(6)\ndays = pd.date_range("2024-01-01", periods=180)\nvisits = 200 + 80*(days.dayofweek >= 5) + rng.normal(0, 20, 180)\ndf = pd.DataFrame({"visits": visits}, index=days)\ndf["weekday"] = df.index.day_name()\nprint(df.groupby("weekday")["visits"].mean().round(0).sort_values())'},
 {cat:'Science',icon:'🌀',title:{en:'Mandelbrot set',de:'Mandelbrot-Menge',fa:'مجموعه مندلبرو'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nw = h = 300\nx = np.linspace(-2.1, 0.8, w)\ny = np.linspace(-1.3, 1.3, h)\nC = x[None, :] + 1j*y[:, None]\nZ = np.zeros_like(C); M = np.zeros(C.shape)\nfor i in range(60):\n    mask = np.abs(Z) <= 2\n    Z[mask] = Z[mask]**2 + C[mask]\n    M += mask\nplt.imshow(M, extent=[-2.1,.8,-1.3,1.3], cmap="twilight_shifted")\nplt.title("Mandelbrot set"); plt.show()'},
 {cat:'Science',icon:'⏱️',title:{en:'ODE: damped pendulum',de:'DGL: gedämpftes Pendel',fa:'معادله دیفرانسیل: آونگ میرا'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\nfrom scipy.integrate import solve_ivp\n\ndef pendulum(t, y, b=0.25, g=9.81, L=1.0):\n    theta, omega = y\n    return [omega, -b*omega - g/L*np.sin(theta)]\n\nsol = solve_ivp(pendulum, [0, 15], [2.0, 0.0], dense_output=True)\nt = np.linspace(0, 15, 400)\nplt.plot(t, sol.sol(t)[0])\nplt.title("Damped pendulum θ(t)")\nplt.xlabel("t [s]"); plt.ylabel("angle [rad]")\nplt.grid(True); plt.show()'},
 {cat:'Science',icon:'🎯',title:{en:'K-means clustering',de:'K-Means-Clustering',fa:'خوشه‌بندی k-means'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\nfrom scipy.cluster.vq import kmeans2\n\nrng = np.random.default_rng(3)\npts = np.vstack([rng.normal(c, .6, (80, 2)) for c in [(0,0), (4,1), (2,4)]])\ncenters, label = kmeans2(pts, 3, seed=1)\n\nplt.scatter(pts[:,0], pts[:,1], c=label, s=14, cmap="viridis")\nplt.scatter(centers[:,0], centers[:,1], marker="X", s=200, c="red")\nplt.title("k-means: 3 clusters found"); plt.show()'},
 {cat:'Science',icon:'📡',title:{en:'Filter a noisy signal (FFT)',de:'Verrauschtes Signal filtern (FFT)',fa:'فیلتر سیگنال نویزی (FFT)'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nt = np.linspace(0, 1, 600, endpoint=False)\nclean = np.sin(2*np.pi*5*t)\nnoisy = clean + np.random.default_rng(0).normal(0, .6, t.size)\n\nspec = np.fft.rfft(noisy)\nfreq = np.fft.rfftfreq(t.size, t[1]-t[0])\nspec[freq > 10] = 0            # low-pass filter!\nfiltered = np.fft.irfft(spec, t.size)\n\nplt.plot(t, noisy, alpha=.3, label="noisy")\nplt.plot(t, filtered, lw=2, label="filtered")\nplt.legend(); plt.title("FFT low-pass filter"); plt.show()'},
 {cat:'Plots',icon:'🏔️',title:{en:'3-D surface plot',de:'3-D-Oberflächenplot',fa:'نمودار سطح سه‌بعدی'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nx = np.linspace(-3, 3, 80)\nX, Y = np.meshgrid(x, x)\nZ = np.exp(-(X**2 + Y**2)/3) * np.cos(2*X) \n\nfig = plt.figure(figsize=(7, 5))\nax = fig.add_subplot(projection="3d")\nax.plot_surface(X, Y, Z, cmap="plasma")\nax.set_title("3-D surface")\nplt.show()'},
 {cat:'Plots',icon:'🕸️',title:{en:'Radar / spider chart',de:'Radar-/Netzdiagramm',fa:'نمودار راداری'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nskills = ["Python", "NumPy", "Pandas", "Plots", "SciPy", "Stats"]\nme     = [9, 7, 6, 8, 5, 6]\nangles = np.linspace(0, 2*np.pi, len(skills), endpoint=False).tolist()\nme += me[:1]; angles += angles[:1]\n\nax = plt.subplot(projection="polar")\nax.plot(angles, me, "o-", color="#4b8bbe")\nax.fill(angles, me, alpha=.25, color="#4b8bbe")\nax.set_xticks(angles[:-1]); ax.set_xticklabels(skills)\nplt.title("Skill radar"); plt.show()'},
 {cat:'Plots',icon:'📶',title:{en:'Error bars',de:'Fehlerbalken',fa:'نوار خطا'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nconditions = ["A", "B", "C", "D"]\nmeans = [4.2, 5.8, 5.1, 6.9]\nerrors = [0.4, 0.6, 0.3, 0.8]\n\nplt.errorbar(conditions, means, yerr=errors, fmt="o", capsize=6,\n             markersize=9, color="#306698", ecolor="#c0356b")\nplt.title("Measurements ± std")\nplt.grid(True, axis="y", alpha=.4)\nplt.show()'},
 {cat:'Plots',icon:'🧱',title:{en:'Stacked area chart',de:'Gestapeltes Flächendiagramm',fa:'نمودار ناحیه انباشته'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nyears = np.arange(2015, 2025)\nweb    = np.array([20, 24, 28, 34, 40, 44, 50, 58, 66, 74])\nmobile = np.array([10, 14, 20, 28, 38, 47, 58, 66, 74, 84])\nother  = np.array([30, 29, 27, 26, 24, 22, 20, 18, 17, 16])\n\nplt.stackplot(years, web, mobile, other,\n              labels=["web", "mobile", "other"],\n              colors=["#4b8bbe", "#2bb5a0", "#f5b027"], alpha=.85)\nplt.legend(loc="upper left"); plt.title("Traffic by channel")\nplt.show()'},
 {cat:'Plots',icon:'🔥',title:{en:'Correlation heatmap',de:'Korrelations-Heatmap',fa:'نقشه حرارتی همبستگی'},
  code:'import pandas as pd\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nrng = np.random.default_rng(1)\nn = 200\nhours  = rng.uniform(0, 10, n)\nscore  = 40 + 5*hours + rng.normal(0, 8, n)\nsleep  = rng.normal(7, 1, n)\ncoffee = 5 - 0.3*sleep + rng.normal(0, .8, n)\ndf = pd.DataFrame({"hours": hours, "score": score, "sleep": sleep, "coffee": coffee})\n\ncorr = df.corr()\nplt.imshow(corr, cmap="RdBu_r", vmin=-1, vmax=1)\nplt.xticks(range(4), corr.columns, rotation=45)\nplt.yticks(range(4), corr.columns)\nfor i in range(4):\n    for j in range(4):\n        plt.text(j, i, f"{corr.iloc[i,j]:.2f}", ha="center", va="center")\nplt.colorbar(); plt.title("Correlation matrix")\nplt.tight_layout(); plt.show()'},
 {cat:'Pandas',icon:'🧾',title:{en:'Pivot table heat view',de:'Pivot-Tabelle als Heatmap',fa:'جدول پیوت به شکل حرارتی'},
  code:'import pandas as pd\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nrng = np.random.default_rng(4)\ndf = pd.DataFrame({\n    "weekday": rng.choice(["Mon","Tue","Wed","Thu","Fri"], 400),\n    "hour":    rng.integers(8, 20, 400),\n    "sales":   rng.poisson(20, 400),\n})\npv = df.pivot_table(index="weekday", columns="hour", values="sales", aggfunc="mean")\npv = pv.reindex(["Mon","Tue","Wed","Thu","Fri"])\nplt.imshow(pv, aspect="auto", cmap="YlOrRd")\nplt.yticks(range(5), pv.index); plt.xticks(range(len(pv.columns)), pv.columns)\nplt.colorbar(label="avg sales"); plt.title("Sales by weekday × hour")\nplt.show()'},
 {cat:'Science',icon:'🚶',title:{en:'2-D random walk',de:'2-D-Zufallsbewegung',fa:'گام تصادفی دوبعدی'},
  code:'import numpy as np\nimport matplotlib.pyplot as plt\n\nrng = np.random.default_rng(12)\nsteps = rng.choice([-1, 1], size=(4000, 2))\npath = steps.cumsum(axis=0)\n\nplt.plot(path[:,0], path[:,1], lw=.5)\nplt.scatter([0],[0], c="green", s=80, zorder=3, label="start")\nplt.scatter(path[-1,0], path[-1,1], c="red", s=80, zorder=3, label="end")\nplt.legend(); plt.title("2-D random walk (4000 steps)")\nplt.axis("equal"); plt.show()'},
 {cat:'Science',icon:'🎰',title:{en:'Monte-Carlo integration',de:'Monte-Carlo-Integration',fa:'انتگرال مونت-کارلو'},
  code:'import numpy as np\n\nrng = np.random.default_rng(0)\nN = 500_000\nx = rng.uniform(0, np.pi, N)\nestimate = np.pi * np.mean(np.sin(x))   # ∫ sin from 0..π = 2\nprint("MC estimate:", round(estimate, 4), " (true: 2)")'},
 {cat:'Python',icon:'🏦',title:{en:'OOP: bank account class',de:'OOP: Bankkonto-Klasse',fa:'شیءگرایی: کلاس حساب بانکی'},
  code:'class Account:\n    def __init__(self, owner):\n        self.owner = owner\n        self.history = []\n\n    def deposit(self, x):  self.history.append(+x); return self\n    def withdraw(self, x): self.history.append(-x); return self\n\n    @property\n    def balance(self):\n        return sum(self.history)\n\nacc = Account("Ada")\nacc.deposit(100).withdraw(30).deposit(55)\nprint(acc.owner, "→", acc.balance, "€")\nprint("transactions:", acc.history)'}
);

/* ================= playground snippet library (per package) ================= */
const PGLIB={
'Python':[
 {t:'f-strings',c:'name, score = "Ada", 97.5\nprint(f"{name}: {score:.1f} points")\nprint(f"{name!r} padded: |{name:>10}|")\nprint(f"binary of 42: {42:b}, hex: {255:x}")'},
 {t:'string methods',c:'s = "  Hello, Python World  "\nprint(s.strip())\nprint(s.lower(), "|", s.upper())\nprint(s.replace("World", "🌍"))\nprint("-".join(["a", "b", "c"]))\nprint("hello world".title(), "abc".startswith("ab"))'},
 {t:'list operations',c:'nums = [3, 1, 4, 1, 5, 9, 2, 6]\nnums.append(5)\nnums.insert(0, 99)\nprint(nums)\nprint("pop:", nums.pop(), "→", nums)\nnums.remove(1)          # first 1 only\nprint(sorted(nums), max(nums), sum(nums))'},
 {t:'slicing tricks',c:'a = list(range(10))\nprint(a[2:8:2])   # start:stop:step\nprint(a[::-1])    # reverse\nprint(a[-3:])     # last three\nb = a[:]          # copy\nb[0] = 999\nprint(a[0], b[0])'},
 {t:'dict methods',c:'d = {"a": 1, "b": 2}\nd["c"] = 3\nprint(d.get("z", "default"))\nprint(list(d.keys()), list(d.values()))\nfor k, v in d.items():\n    print(k, "→", v)\nd2 = {**d, "b": 20}   # merge/override\nprint(d2)'},
 {t:'sets',c:'a = {1, 2, 3, 4}\nb = {3, 4, 5, 6}\nprint("union:       ", a | b)\nprint("intersection:", a & b)\nprint("difference:  ", a - b)\nprint("sym. diff:   ", a ^ b)\nprint(3 in a, 99 in a)'},
 {t:'comprehensions',c:'squares = [x*x for x in range(10)]\nevens   = [x for x in range(20) if x % 2 == 0]\npairs   = [(i, j) for i in "ab" for j in "12"]\nd       = {w: len(w) for w in ["hi", "hello", "hey"]}\nprint(squares)\nprint(evens)\nprint(pairs)\nprint(d)'},
 {t:'enumerate & zip',c:'names  = ["Ada", "Alan", "Grace"]\nyears  = [1815, 1912, 1906]\nfor i, name in enumerate(names, start=1):\n    print(i, name)\nfor n, y in zip(names, years):\n    print(f"{n} was born in {y}")\nprint(dict(zip(names, years)))'},
 {t:'sorting with key',c:'words = ["banana", "fig", "Apple", "date"]\nprint(sorted(words))                     # case-sensitive!\nprint(sorted(words, key=str.lower))\nprint(sorted(words, key=len, reverse=True))\npeople = [("Ada", 36), ("Alan", 41), ("Grace", 85)]\nprint(sorted(people, key=lambda p: p[1]))'},
 {t:'map / filter / lambda',c:'nums = [1, 2, 3, 4, 5, 6]\ndoubled = list(map(lambda x: 2*x, nums))\nodds    = list(filter(lambda x: x % 2, nums))\nprint(doubled)\nprint(odds)\nprint(list(map(str.upper, ["a", "b"])))'},
 {t:'exceptions',c:'def parse(x):\n    try:\n        return int(x)\n    except ValueError as e:\n        print("  cannot parse:", e)\n        return None\n\nprint(parse("42"))\nprint(parse("hello"))\n\ntry:\n    raise RuntimeError("custom error!")\nexcept RuntimeError as e:\n    print("caught:", e)'},
 {t:'classes',c:'class Point:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n    def __repr__(self):\n        return f"Point({self.x}, {self.y})"\n    def __add__(self, other):\n        return Point(self.x + other.x, self.y + other.y)\n\np = Point(1, 2) + Point(3, 4)\nprint(p)'},
 {t:'dataclasses',c:'from dataclasses import dataclass\n\n@dataclass\nclass Book:\n    title: str\n    pages: int\n    read: bool = False\n\nb = Book("Fluent Python", 792)\nprint(b)\nb.read = True\nprint(b.read, b.pages > 500)'},
 {t:'generators',c:'def countdown(n):\n    while n > 0:\n        yield n\n        n -= 1\n\nprint(list(countdown(5)))\n\ngen = (x*x for x in range(1_000_000))  # lazy, no memory!\nprint(next(gen), next(gen), next(gen))\nprint(sum(x for x in range(101)))'},
 {t:'itertools',c:'import itertools as it\n\nprint(list(it.combinations("ABCD", 2)))\nprint(list(it.permutations("AB")))\nprint(list(it.islice(it.count(10, 5), 4)))\nprint(list(it.chain([1, 2], [3, 4])))\nfor k, grp in it.groupby("aaabbc"):\n    print(k, len(list(grp)))'},
 {t:'collections',c:'from collections import Counter, defaultdict\n\nc = Counter("mississippi")\nprint(c.most_common(3))\n\nd = defaultdict(list)\nfor word in ["apple", "avocado", "banana", "blueberry"]:\n    d[word[0]].append(word)\nprint(dict(d))'},
 {t:'datetime',c:'from datetime import date, timedelta\n\ntoday = date(2026, 7, 9)\nbirthday = date(2026, 12, 24)\ndiff = birthday - today\nprint("days to go:", diff.days)\nprint("in 90 days:", today + timedelta(days=90))\nprint(today.strftime("%A, %d %B %Y"))'},
 {t:'json',c:'import json\n\ndata = {"name": "Ada", "skills": ["math", "python"], "level": 9}\ns = json.dumps(data, indent=2)\nprint(s)\nback = json.loads(s)\nprint(back["skills"][1], type(back))'},
 {t:'recursion',c:'def factorial(n):\n    return 1 if n <= 1 else n * factorial(n - 1)\n\ndef fib(n, memo={}):\n    if n < 2: return n\n    if n not in memo:\n        memo[n] = fib(n-1) + fib(n-2)\n    return memo[n]\n\nprint("10! =", factorial(10))\nprint("fib(30) =", fib(30))'},
],
'NumPy':[
 {t:'array creation',c:'import numpy as np\nprint(np.array([1, 2, 3]))\nprint(np.zeros((2, 3)))\nprint(np.ones(4))\nprint(np.full((2, 2), 7))\nprint(np.eye(3))'},
 {t:'arange & linspace',c:'import numpy as np\nprint(np.arange(0, 20, 5))\nprint(np.linspace(0, 1, 6))\nprint(np.logspace(0, 3, 4))    # 1, 10, 100, 1000\nprint(np.arange(12).reshape(3, 4))'},
 {t:'dtype & astype',c:'import numpy as np\na = np.array([1.7, 2.3, 3.9])\nprint(a.dtype)\nprint(a.astype(int))            # truncates!\nprint(np.round(a).astype(int))\nb = np.array([1, 2, 3], dtype=np.float32)\nprint(b.dtype, b.nbytes, "bytes")'},
 {t:'2-D indexing',c:'import numpy as np\nm = np.arange(1, 13).reshape(3, 4)\nprint(m)\nprint("row 0:   ", m[0])\nprint("col 1:   ", m[:, 1])\nprint("center:  ", m[1, 1:3])\nprint("corners:\\n", m[::2, ::3])'},
 {t:'boolean masks',c:'import numpy as np\nx = np.array([12, 5, 8, 20, 3, 15])\nprint(x[x > 10])\nprint(x[(x > 4) & (x < 16)])   # & not "and"!\nx[x < 6] = 0\nprint(x)\nprint("how many > 10:", (x > 10).sum())'},
 {t:'fancy indexing',c:'import numpy as np\nx = np.array([10, 20, 30, 40, 50])\nidx = [0, 3, 3, 4]\nprint(x[idx])\norder = np.argsort([3, 1, 2])\nprint(order, x[:3][order])'},
 {t:'broadcasting',c:'import numpy as np\nrow = np.array([1, 2, 3])\ncol = np.array([[10], [20], [30]])\nprint(row + col)        # 3x3!\nm = np.ones((3, 3))\nprint(m * row)\nprint((m.T * row).T)'},
 {t:'aggregations & axis',c:'import numpy as np\nm = np.array([[4, 9, 2], [7, 1, 6]])\nprint("all:", m.sum(), m.mean().round(2))\nprint("per col:", m.sum(axis=0))\nprint("per row:", m.max(axis=1))\nprint("argmax:", m.argmax(), "→ pos", np.unravel_index(m.argmax(), m.shape))'},
 {t:'sort & argsort',c:'import numpy as np\nx = np.array([30, 10, 50, 20])\nprint(np.sort(x))\nprint(np.argsort(x))       # indexes that would sort\nnames = np.array(["c", "a", "d", "b"])\nprint(names[np.argsort(x)])'},
 {t:'unique & counts',c:'import numpy as np\nvotes = np.array(["a","b","a","c","b","a","a"])\nvals, counts = np.unique(votes, return_counts=True)\nfor v, c in zip(vals, counts):\n    print(v, "→", c)\nprint("winner:", vals[counts.argmax()])'},
 {t:'np.where & select',c:'import numpy as np\ntemp = np.array([28, 12, 33, 5, 21])\nprint(np.where(temp > 20, "warm", "cold"))\ngrade = np.select([temp >= 30, temp >= 15], ["hot", "mild"], "cold")\nprint(grade)\nprint(np.clip(temp, 10, 30))'},
 {t:'stack & concatenate',c:'import numpy as np\na = np.array([1, 2, 3]); b = np.array([4, 5, 6])\nprint(np.concatenate([a, b]))\nprint(np.vstack([a, b]))\nprint(np.hstack([a.reshape(-1,1), b.reshape(-1,1)]))\nprint(np.column_stack([a, b]))'},
 {t:'random generator',c:'import numpy as np\nrng = np.random.default_rng(42)\nprint(rng.integers(1, 7, 8))          # dice\nprint(rng.random(3).round(3))\nprint(rng.normal(100, 15, 4).round(1))\nprint(rng.choice(["red","green","blue"], 5))\nprint(rng.permutation(np.arange(6)))'},
 {t:'cumsum & diff',c:'import numpy as np\nx = np.array([1, 3, 6, 10, 15])\nprint("cumsum:", np.cumsum([1, 2, 3, 4, 5]))\nprint("diff:  ", np.diff(x))\nprint("cumprod:", np.cumprod([1, 2, 3, 4]))'},
 {t:'percentile & stats',c:'import numpy as np\nrng = np.random.default_rng(1)\ndata = rng.normal(50, 10, 1000)\nprint("mean/median:", data.mean().round(2), np.median(data).round(2))\nprint("std:", data.std().round(2))\nfor q in [25, 50, 75, 95]:\n    print(f"p{q}: {np.percentile(data, q):.1f}")'},
 {t:'linalg: solve & det',c:'import numpy as np\nA = np.array([[3., 1.], [1., 2.]])\nb = np.array([9., 8.])\nprint("x =", np.linalg.solve(A, b))\nprint("det =", np.linalg.det(A))\nprint("inv:\\n", np.linalg.inv(A).round(3))'},
 {t:'eigenvalues',c:'import numpy as np\nA = np.array([[2., 0.], [1., 3.]])\nw, v = np.linalg.eig(A)\nprint("eigenvalues: ", w)\nprint("eigenvectors:\\n", v.round(3))\nprint("check A·v = λ·v:", np.allclose(A @ v[:,0], w[0]*v[:,0]))'},
 {t:'matrix power & norm',c:'import numpy as np\nA = np.array([[1, 1], [1, 0]])\nprint(np.linalg.matrix_power(A, 10))   # Fibonacci!\nv = np.array([3., 4.])\nprint("norm:", np.linalg.norm(v))\nprint("normalized:", (v / np.linalg.norm(v)).round(3))'},
],
'SciPy':[
 {t:'norm pdf & cdf',c:'from scipy import stats\nprint("P(x<1.96):", stats.norm.cdf(1.96).round(4))\nprint("95% quantile:", stats.norm.ppf(0.975).round(3))\nprint("density at 0:", stats.norm.pdf(0).round(4))\nprint("IQ>130:", (1 - stats.norm.cdf(130, 100, 15)).round(4))'},
 {t:'describe data',c:'from scipy import stats\nimport numpy as np\ndata = np.random.default_rng(0).normal(10, 2, 500)\nd = stats.describe(data)\nprint("n:", d.nobs)\nprint("mean:", round(d.mean, 3), " var:", round(d.variance, 3))\nprint("min/max:", np.round(d.minmax, 2))\nprint("skew:", round(d.skewness, 3))'},
 {t:'t-test',c:'from scipy import stats\nimport numpy as np\nrng = np.random.default_rng(1)\na = rng.normal(10.0, 2, 60)\nb = rng.normal(11.2, 2, 60)\nt, p = stats.ttest_ind(a, b)\nprint(f"t={t:.2f}  p={p:.4f}")\nprint("significant at 5%:", p < 0.05)'},
 {t:'correlation (pearson)',c:'from scipy import stats\nimport numpy as np\nrng = np.random.default_rng(2)\nx = rng.random(100)\ny = 2*x + rng.normal(0, .2, 100)\nr, p = stats.pearsonr(x, y)\nprint(f"r = {r:.3f}, p = {p:.2e}")\nrho, _ = stats.spearmanr(x, y)\nprint(f"spearman rho = {rho:.3f}")'},
 {t:'binomial & poisson',c:'from scipy import stats\n# 10 coin flips: P(exactly 7 heads)?\nprint("P(7 heads):", stats.binom.pmf(7, 10, 0.5).round(4))\nprint("P(≥8 heads):", (1 - stats.binom.cdf(7, 10, 0.5)).round(4))\n# shop: 4 customers/hour on average → P(0 in an hour)?\nprint("P(0 customers):", stats.poisson.pmf(0, 4).round(4))'},
 {t:'minimize',c:'from scipy import optimize\nimport numpy as np\nf = lambda x: (x - 2)**2 + np.sin(5*x)\nres = optimize.minimize_scalar(f, bounds=(0, 4), method="bounded")\nprint("min at x =", round(res.x, 4))\nprint("f(x) =", round(res.fun, 4))'},
 {t:'curve_fit',c:'import numpy as np\nfrom scipy.optimize import curve_fit\nrng = np.random.default_rng(0)\nx = np.linspace(0, 10, 50)\ny = 3*np.sin(0.8*x) + rng.normal(0, .3, 50)\nf = lambda x, a, w: a*np.sin(w*x)\n(a, w), _ = curve_fit(f, x, y, p0=[1, 1])\nprint(f"fitted: {a:.2f}·sin({w:.2f}·x)")'},
 {t:'root finding',c:'from scipy import optimize\nimport numpy as np\n# where does x·e^x = 5 ?\nroot = optimize.brentq(lambda x: x*np.exp(x) - 5, 0, 3)\nprint("x =", round(root, 5))\nprint("check:", round(root*np.exp(root), 3))'},
 {t:'integrate quad',c:'from scipy import integrate\nimport numpy as np\narea, err = integrate.quad(lambda x: np.exp(-x**2), -np.inf, np.inf)\nprint("Gauss integral:", round(area, 6))\nprint("√π           :", round(np.sqrt(np.pi), 6))\narea2, _ = integrate.quad(np.sin, 0, np.pi)\nprint("∫sin 0..π =", round(area2, 6))'},
 {t:'solve ODE',c:'from scipy.integrate import solve_ivp\nimport numpy as np\n# exponential cooling: T\' = -0.3(T - 20)\nsol = solve_ivp(lambda t, T: -0.3*(T - 20), [0, 10], [90],\n                t_eval=np.linspace(0, 10, 6))\nfor t, T in zip(sol.t, sol.y[0]):\n    print(f"t={t:4.1f}  T={T:5.1f}°C")'},
 {t:'interpolate',c:'from scipy import interpolate\nimport numpy as np\nx = np.array([0, 1, 2, 3, 4])\ny = np.array([0, 2, 1, 3, 2])\nf = interpolate.interp1d(x, y, kind="cubic")\nxs = np.linspace(0, 4, 9)\nprint(np.round(f(xs), 2))'},
 {t:'find peaks',c:'from scipy.signal import find_peaks\nimport numpy as np\nt = np.linspace(0, 6*np.pi, 300)\nsig = np.sin(t) + 0.4*np.sin(3.3*t)\npeaks, props = find_peaks(sig, height=0.5)\nprint("peaks found:", len(peaks))\nprint("at t ≈", np.round(t[peaks], 2))'},
 {t:'k-means',c:'from scipy.cluster.vq import kmeans2\nimport numpy as np\nrng = np.random.default_rng(5)\npts = np.vstack([rng.normal(0, .5, (50, 2)),\n                 rng.normal(3, .5, (50, 2))])\ncenters, labels = kmeans2(pts, 2, seed=2)\nprint("centers:\\n", centers.round(2))\nprint("cluster sizes:", np.bincount(labels))'},
 {t:'distance matrix',c:'from scipy.spatial import distance_matrix\nimport numpy as np\ncities = np.array([[0, 0], [3, 4], [6, 8]])\nD = distance_matrix(cities, cities)\nprint(D.round(2))\nprint("farthest pair:", np.unravel_index(D.argmax(), D.shape))'},
],
'Pandas':[
 {t:'Series basics',c:'import pandas as pd\ns = pd.Series([10, 20, 30], index=["a", "b", "c"])\nprint(s)\nprint(s["b"], s.mean(), s.idxmax())\nprint(s * 2)'},
 {t:'DataFrame create',c:'import pandas as pd\ndf = pd.DataFrame({\n    "name": ["Ada", "Alan", "Grace"],\n    "age":  [36, 41, 85],\n    "field":["math", "cs", "cs"],\n})\nprint(df)\nprint()\nprint(df.shape, list(df.columns))'},
 {t:'head / info / describe',c:'import pandas as pd\nimport numpy as np\ndf = pd.DataFrame(np.random.default_rng(0).normal(50, 10, (100, 3)),\n                  columns=["a", "b", "c"])\nprint(df.head(3).round(1))\nprint()\nprint(df.describe().round(1))'},
 {t:'loc & iloc',c:'import pandas as pd\ndf = pd.DataFrame({"x": [1, 2, 3], "y": [10, 20, 30]},\n                  index=["r1", "r2", "r3"])\nprint(df.loc["r2"])          # by label\nprint(df.iloc[0])            # by position\nprint(df.loc["r1":"r2", "y"])\nprint(df.iloc[0:2, 1])'},
 {t:'filtering',c:'import pandas as pd\ndf = pd.DataFrame({"city": ["Berlin","Munich","Hamburg","Cologne"],\n                   "pop": [3.7, 1.5, 1.9, 1.1]})\nprint(df[df["pop"] > 1.4])\nprint()\nprint(df.query("pop > 1.4 and city != \'Berlin\'"))\nprint(df[df.city.isin(["Berlin", "Cologne"])])'},
 {t:'new columns',c:'import pandas as pd\ndf = pd.DataFrame({"price": [100, 250, 80], "qty": [3, 1, 5]})\ndf["total"] = df.price * df.qty\ndf["expensive"] = df.price > 90\ndf = df.assign(tax=lambda d: d.total * 0.19)\nprint(df)'},
 {t:'sort & rank',c:'import pandas as pd\ndf = pd.DataFrame({"team": list("ABCD"), "pts": [7, 12, 7, 3]})\nprint(df.sort_values("pts", ascending=False))\ndf["rank"] = df.pts.rank(ascending=False, method="min")\nprint(df)\nprint(df.nlargest(2, "pts"))'},
 {t:'value_counts',c:'import pandas as pd\ns = pd.Series(["red","blue","red","green","red","blue"])\nprint(s.value_counts())\nprint()\nprint(s.value_counts(normalize=True).round(2))\nprint(s.nunique(), "unique colors")'},
 {t:'groupby & agg',c:'import pandas as pd\ndf = pd.DataFrame({\n    "dept":   ["IT","IT","HR","HR","Sales"],\n    "salary": [70, 85, 50, 55, 60],\n})\nprint(df.groupby("dept")["salary"].mean())\nprint()\nprint(df.groupby("dept")["salary"].agg(["count", "mean", "max"]))'},
 {t:'pivot_table',c:'import pandas as pd\ndf = pd.DataFrame({\n    "month":  ["Jan","Jan","Feb","Feb"],\n    "product":["A","B","A","B"],\n    "sales":  [10, 15, 12, 18],\n})\nprint(df.pivot_table(index="month", columns="product", values="sales"))'},
 {t:'merge (join)',c:'import pandas as pd\nusers  = pd.DataFrame({"uid": [1, 2, 3], "name": ["Ada","Alan","Grace"]})\norders = pd.DataFrame({"uid": [1, 1, 3], "total": [50, 30, 120]})\nprint(pd.merge(users, orders, on="uid", how="left"))'},
 {t:'concat',c:'import pandas as pd\nq1 = pd.DataFrame({"month": ["Jan","Feb"], "rev": [10, 12]})\nq2 = pd.DataFrame({"month": ["Mar","Apr"], "rev": [14, 11]})\nboth = pd.concat([q1, q2], ignore_index=True)\nprint(both)\nprint("total:", both.rev.sum())'},
 {t:'missing values',c:'import pandas as pd\nimport numpy as np\ndf = pd.DataFrame({"a": [1, np.nan, 3], "b": [np.nan, 5, 6]})\nprint(df.isna().sum())\nprint(df.fillna(0))\nprint(df.dropna())\nprint(df.fillna(df.mean()).round(1))'},
 {t:'apply & map',c:'import pandas as pd\ndf = pd.DataFrame({"name": ["ada lovelace", "alan turing"]})\ndf["proper"] = df.name.apply(str.title)\ndf["initials"] = df.name.map(lambda s: "".join(w[0].upper() for w in s.split()))\nprint(df)'},
 {t:'.str accessor',c:'import pandas as pd\ns = pd.Series(["apple pie", "banana split", "cherry cake"])\nprint(s.str.upper())\nprint(s.str.contains("an"))\nprint(s.str.split().str[0])\nprint(s.str.len())'},
 {t:'.dt accessor',c:'import pandas as pd\ndates = pd.Series(pd.date_range("2024-12-28", periods=6))\ndf = pd.DataFrame({"date": dates})\ndf["weekday"] = df.date.dt.day_name()\ndf["month"]   = df.date.dt.month\ndf["is_weekend"] = df.date.dt.dayofweek >= 5\nprint(df)'},
 {t:'resample',c:'import pandas as pd\nimport numpy as np\ndays = pd.date_range("2024-01-01", periods=60)\nts = pd.Series(np.arange(60) + np.random.default_rng(0).normal(0, 5, 60), index=days)\nprint("weekly mean:")\nprint(ts.resample("W").mean().round(1).head())\nprint("monthly max:")\nprint(ts.resample("ME").max().round(1))'},
 {t:'rolling window',c:'import pandas as pd\ns = pd.Series([1, 3, 2, 6, 4, 8, 7, 12])\nprint(pd.DataFrame({\n    "raw": s,\n    "roll3_mean": s.rolling(3).mean(),\n    "roll3_max":  s.rolling(3).max(),\n    "expanding":  s.expanding().mean().round(2),\n}))'},
],
'Matplotlib':[
 {t:'line styles',c:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.linspace(0, 10, 60)\nplt.plot(x, np.sin(x), "b-",  label="solid")\nplt.plot(x, np.sin(x-1), "r--", label="dashed")\nplt.plot(x, np.sin(x-2), "g:o", markersize=3, label="dotted+o")\nplt.legend(); plt.show()'},
 {t:'legend & grid',c:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.linspace(0, 2*np.pi, 100)\nfor k in [1, 2, 3]:\n    plt.plot(x, np.sin(k*x)/k, label=f"sin({k}x)/{k}")\nplt.legend(loc="upper right", ncol=3)\nplt.grid(True, alpha=.4)\nplt.title("Harmonics"); plt.show()'},
 {t:'scatter + colormap',c:'import numpy as np\nimport matplotlib.pyplot as plt\nrng = np.random.default_rng(0)\nx, y = rng.random(150), rng.random(150)\nsize = 300 * rng.random(150)\nplt.scatter(x, y, s=size, c=x+y, cmap="plasma", alpha=.6, edgecolors="w")\nplt.colorbar(label="x+y")\nplt.title("Bubble scatter"); plt.show()'},
 {t:'bar & barh',c:'import matplotlib.pyplot as plt\nlangs = ["Python", "JS", "Rust", "Go"]\nusage = [52, 45, 18, 14]\nfig, ax = plt.subplots(1, 2, figsize=(9, 3.5))\nax[0].bar(langs, usage, color="#4b8bbe")\nax[1].barh(langs, usage, color="#2bb5a0")\nplt.tight_layout(); plt.show()'},
 {t:'grouped bars',c:'import numpy as np\nimport matplotlib.pyplot as plt\nlabels = ["Q1", "Q2", "Q3", "Q4"]\na = [20, 34, 30, 35]; b = [25, 32, 34, 20]\nx = np.arange(4); w = 0.35\nplt.bar(x - w/2, a, w, label="2023")\nplt.bar(x + w/2, b, w, label="2024")\nplt.xticks(x, labels); plt.legend()\nplt.title("Grouped bars"); plt.show()'},
 {t:'histogram',c:'import numpy as np\nimport matplotlib.pyplot as plt\nrng = np.random.default_rng(3)\nplt.hist(rng.normal(0, 1, 800), bins=40, alpha=.7, label="σ=1")\nplt.hist(rng.normal(0, 2, 800), bins=40, alpha=.5, label="σ=2")\nplt.legend(); plt.title("Two distributions"); plt.show()'},
 {t:'pie chart',c:'import matplotlib.pyplot as plt\nplt.pie([40, 30, 20, 10],\n        labels=["work", "sleep", "code", "fun"],\n        autopct="%1.0f%%", startangle=90,\n        explode=[0, 0, 0.1, 0],\n        colors=["#4b8bbe","#2bb5a0","#f5b027","#c0356b"])\nplt.title("A developer day"); plt.show()'},
 {t:'boxplot',c:'import numpy as np\nimport matplotlib.pyplot as plt\nrng = np.random.default_rng(7)\ndata = [rng.normal(m, s, 200) for m, s in [(5,1),(7,2),(6,0.5)]]\nplt.boxplot(data)\nplt.xticks([1,2,3], ["A", "B", "C"])\nplt.title("Boxplots"); plt.show()'},
 {t:'violin',c:'import numpy as np\nimport matplotlib.pyplot as plt\nrng = np.random.default_rng(1)\ndata = [np.concatenate([rng.normal(4,1,150), rng.normal(8,1,150)]),\n        rng.normal(6, 1.5, 300)]\nplt.violinplot(data, showmedians=True)\nplt.xticks([1,2], ["bimodal", "normal"])\nplt.title("Violin plots show shape!"); plt.show()'},
 {t:'stackplot',c:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.arange(2018, 2026)\ncloud = [5, 8, 13, 20, 28, 37, 45, 52]\nonprem = [40, 38, 35, 31, 27, 22, 18, 15]\nplt.stackplot(x, cloud, onprem, labels=["cloud", "on-prem"],\n              colors=["#4b8bbe", "#f5b027"], alpha=.8)\nplt.legend(loc="upper left"); plt.show()'},
 {t:'fill_between',c:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.linspace(0, 10, 200)\ny = np.sin(x)\nplt.plot(x, y, "k")\nplt.fill_between(x, y, 0, where=y>0, color="#2bb5a0", alpha=.5, label="positive")\nplt.fill_between(x, y, 0, where=y<0, color="#c0356b", alpha=.5, label="negative")\nplt.legend(); plt.show()'},
 {t:'errorbar',c:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.arange(1, 8)\ny = np.array([2.1, 3.4, 4.2, 5.4, 5.9, 7.2, 7.8])\nplt.errorbar(x, y, yerr=0.4+0.1*x, fmt="s-", capsize=5, color="#306698")\nplt.title("Measurement ± growing error"); plt.show()'},
 {t:'twin axes',c:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.arange(1, 13)\nfig, ax = plt.subplots()\nax.plot(x, 15+10*np.sin(x/2), "r-o"); ax.set_ylabel("temp °C", color="r")\nax2 = ax.twinx()\nax2.bar(x, 50+30*np.cos(x/2), alpha=.3); ax2.set_ylabel("rain mm", color="b")\nplt.show()'},
 {t:'subplots grid',c:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.linspace(0, 2*np.pi, 100)\nfig, axs = plt.subplots(2, 2, figsize=(8, 5))\nfor ax, f in zip(axs.flat, [np.sin, np.cos, np.tan, np.sinc]):\n    ax.plot(x, np.clip(f(x), -3, 3))\n    ax.set_title(f.__name__)\nplt.tight_layout(); plt.show()'},
 {t:'annotate & text',c:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.linspace(0, 10, 200)\ny = np.sin(x)*np.exp(-x/6)\nplt.plot(x, y)\ni = np.argmax(y)\nplt.annotate("maximum!", xy=(x[i], y[i]), xytext=(x[i]+2, y[i]),\n             arrowprops=dict(arrowstyle="->", color="red"))\nplt.text(6, -0.5, "damped sine", fontsize=12, style="italic")\nplt.show()'},
 {t:'imshow heatmap',c:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.linspace(-2, 2, 150)\nX, Y = np.meshgrid(x, x)\nZ = np.exp(-(X**2+Y**2)) * np.cos(3*X) \nplt.imshow(Z, extent=[-2,2,-2,2], cmap="coolwarm", origin="lower")\nplt.colorbar(); plt.title("2-D function"); plt.show()'},
 {t:'polar plot',c:'import numpy as np\nimport matplotlib.pyplot as plt\ntheta = np.linspace(0, 2*np.pi, 400)\nr = 1 + 0.6*np.sin(5*theta)\nax = plt.subplot(projection="polar")\nax.plot(theta, r, color="#7b5fe0")\nax.fill(theta, r, alpha=.25, color="#7b5fe0")\nplt.title("Polar flower"); plt.show()'},
 {t:'3-D surface',c:'import numpy as np\nimport matplotlib.pyplot as plt\nx = np.linspace(-2, 2, 60)\nX, Y = np.meshgrid(x, x)\nZ = X*np.exp(-X**2-Y**2)\nax = plt.axes(projection="3d")\nax.plot_surface(X, Y, Z, cmap="viridis")\nax.view_init(30, 45)\nplt.show()'},
 {t:'log scale',c:'import numpy as np\nimport matplotlib.pyplot as plt\nn = np.arange(1, 21)\nfig, ax = plt.subplots(1, 2, figsize=(9, 3.5))\nax[0].plot(n, 2.0**n, "o-"); ax[0].set_title("linear scale")\nax[1].semilogy(n, 2.0**n, "o-", color="#c0356b"); ax[1].set_title("log scale")\nplt.tight_layout(); plt.show()'},
]};
